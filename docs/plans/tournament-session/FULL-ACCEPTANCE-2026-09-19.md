# 锦标赛连续牌桌全面验收报告

日期：2026-09-19。范围：本轮连续牌桌与此前46–65修复的完整回归，以手机触控为重点，同时检查电脑。未使用子代理，未提交Git，未部署。

## 结论

本地软件验收通过，可以进入朋友实机试用。补测发现的9组问题（66–74）已修复。原101个验收ID中，99项在注明的测试层级通过；B-04、U-11的浏览器模拟部分通过，真实手机软键盘及系统行为仍需实机确认。不能将这两项写成全部通过。

此结论针对现有需求与列明场景，不代表所有设备、所有下注组合或长时间在线负载均经过穷举。需求仍是6/8/9个物理座位；人数随淘汰支持2–9人。熟人功能保留、默认关闭。

## 本轮新增问题与修复

| 编号 | 问题 | 发生场景 | 交付方案及结果 |
|---|---|---|---|
| 66 / P2 | 开启熟人记牌后，换手不自动发送记录 | commitSettlement仍持有提交锁时调用syncLearningJobs，被锁直接返回；刷新才可能补发 | 提交锁释放后发送，真实浏览器断言只发1次，原handId/context/匿名UID正确，成功清除原任务 |
| 67 / P1 | 损坏的嵌套存档被误判为有效，可能造成恢复白屏 | participants、ops、结算行、历史摘要、学习任务或人员编辑内含null/错误结构 | 保存与加载共同校验嵌套结构；损坏及未来schema保留原文本并显示恢复/导出入口；新增领域和浏览器恢复测试 |
| 68 / P2 | 旧配置恢复丢失桌型，非整BB筹码被舍入 | 旧9座、10001筹码预填为默认6座，筹码先转一位小数BB再建桌 | 恢复容量；以整数筹码预填保留10001，熟人功能仍关闭；真实旧配置表单通过 |
| 69 / P2 | 未知余额尚未填完，下一手按钮仍显示可提交 | 未知摊牌只填写一人的余额 | 仍有null余额时disabled，完整填写后可提交；F3两名未知玩家逐行输入验证 |
| 70 / P2 | 手机九座轻微重叠、公共牌靠近座位、选牌目标过小 | 320px九人桌；13列牌库；刷新后已选底牌但牌库重新展开 | 缩小座位固定宽度，约束桌心宽度，手机牌库44px触控目标与16px输入文字；恢复已有底牌自动折叠；45种布局检测及截图复查 |
| 71 / P2 | 实际余额输入后，牌桌仍显示待核对，结算仍标等待行动 | 在结算表连续改余额或手动结束本手 | 输入时只更新牌桌与候选名单，保留输入焦点；等待行动徽标仅在playing显示；金额和焦点断言通过 |
| 72 / P1 | 已结束牌谱取消结算后立即又进入结算，不能纠错 | 最后一步录错后点击“取消，继续纠正本手” | 提供结束牌谱的纠错状态，可撤销最后一步或返回核对；不改handId、手数和已确认余额；真实撤销并改为跟注通过 |
| 73 / P2 | v2动作混入旧index字段没有拒绝 | seat_id与index同时提交 | 与seat/seat_index统一拒绝为400，不默默选择某字段；新增HTTP测试 |
| 74 / P2 | 手机快速选牌后取消可能漏点击，退出焦点不稳定 | 打开牌库的平滑滚动尚未结束；触控按下在按钮上，抬起落到移动后的文字上 | 牌库即时定位，交互目标使用touch-action:manipulation，取消后下一帧恢复焦点；确认公共牌后移动端折叠牌库并回动作控件；触控完整四街与延迟建议交错通过 |

## 验收证据

- `python -X utf8 _verify_all.py`：176项pytest通过，54项Node测试通过，退出0。pytest耗时183.73秒；2条FastAPI/Starlette依赖弃用警告，不是产品失败。
- `node tests/browser/session.e2e.mjs`：原有真实浏览器回归通过。包含四街、草稿/升盲、配额失败、淘汰和英雄结束、两个标签页冲突、断网和建议重试。
- `node tests/browser/acceptance.e2e.mjs`：新增17组浏览器场景通过。手机使用触控事件，桌面使用鼠标；真实API响应的正文读取可控延迟，故障仅注入测试上下文。
- 320、375、390、768、1440宽度，各检查建桌、进行中、结算，以及6座、9座、两人带空位，共45种布局。断言文档无横向溢出、座位相互不碰撞、座位不覆盖公共牌/底池；关键截图实际打开复查。截图前等待相应业务状态，设备模式切换引起的页面重载也等待恢复完成。
- 领域连续105次提交，验证只保留100条摘要，105个待重试学习任务、当前手和lastCommit不丢。这是领域历史窗口测试，不冒充105手完整浏览器牌谱或长时间压力测试。
- 额外验证测试汇总中注入失败返回非零；把reset_user替换为错误的reset_all后，命名空间回归测试确实失败；令牌环境原本存在/不存在两种情况均完整恢复。
- 全流程未操作日常浏览器存储，统计服务使用独立临时文件。默认关闭模式的三手流程以Node侧跨刷新Network累计验证统计请求总数0。

逐项依据见 [101项覆盖矩阵](./ACCEPTANCE-STATUS-2026-09-19.md)。浏览器结果见 [扩展结果](../../screenshots/full-acceptance-2026-09-19/results.json) 与 [原回归结果](../../screenshots/session-fix-2026-09-19/results.json)。执行入口与环境配置见 [浏览器测试说明](../../../tests/browser/README.md)。

## 验收口径

1. 用户已接受简化建桌：自己初始固定底部1号座，其他玩家输入实际整数筹码。因此旧B-01的英雄5号座由领域F1测试覆盖，浏览器执行自己1号座的等价连续流程；C-18的30BB按6000筹码表单验证，L-06不再要求重复选择英雄座位。
2. 动作采用后端验证成功后才写入history。S-13的board 400没有已提交动作需要回滚，错误时保留原历史，旧请求以requestId隔离。实际重新选牌及3/1/1全程通过。
3. UI、API及领域测试各自说明覆盖层级；测试数量不等于验收ID数量。54项Node测试含11项旧logic回归，其通过不代替当前app.js浏览器验证。
4. 失败截图和日志保留用于追溯。`failure-learning`记录66；`failure-responsive`记录70。`failure-f3-showdown`在调试中也出现过测试脚本提前读取动作数导致的超时，最后一份该文件是脚本时序问题；已修正为等待当前行动按钮可用后才取动作基线。触控滚动问题另由诊断事件序列确认，不把所有失败都归为产品错误。

## 仍需实机确认

仅靠本机Chromium不能验证Android/iPhone的真实软键盘、输入法、浏览器地址栏和系统字体缩放，也未运行Safari/WebKit实机。B-04与U-11明确保留这部分，不需要用户现在提供配置。

朋友最后试用时：连续录10–20手，其中至少一次改实际筹码、一次有人离桌/淘汰、一次升盲、一次刷新恢复；手机展开加注/结算输入，确认键盘不挡错误与确认入口。重点反馈设备、浏览器、具体步骤、预期和实际结果。无需提供账号或真实统计数据。

没有运行生产部署、持续数小时的压力/内存泄漏测试或未纳入pytest的旧独立压力脚本。算法覆盖以既有权益、分层EV、ICM和CFR回归为准，未增加策略收益评估。

## 资源收尾

- 隔离服务PID37308已停止，8142端口无监听。Ctrl+C返回1为主动停止服务，不是测试失败。
- 本轮记录的浏览器PID15740、25180、33412、33188、16204、36664、27632、35120、26156、13644、6476、35660、30272、13136、8072、6472、25752、36060、38772、36008、15696、31696、23744均已退出；测试脚本finally执行browser.close。
- 原有8765服务PID16608仍在运行，未停止或重启。重启原项目服务后可加载新后端代码，日常地址为 `http://127.0.0.1:8765`。
- 真实 `backend/data/opponents.json` 前后SHA256相同：`9fc73f972ac7cfadd3aa8d60c7a398fb40d5ac183bc367acaf0d239ab859974e`。
- 已核对以下6个目录均为本轮创建且位于 `C:\Users\Administrator\AppData\Local\Temp`，删除操作被自动审批规则拒绝，工具仅返回 `blocked by policy`，没有执行删除、没有改用其他途径绕过：`poker-session-qa-_usgggea`、`poker-acceptance-profile-lhxJ5K`、`poker-acceptance-profile-DIbVfd`、`poker-acceptance-profile-LmCGyQ`、`poker-acceptance-profile-Kigsvw`、`poker-acceptance-profile-Rf2JEI`。本轮已清理路径数为0。
- 交付截图、结果JSON、失败证据保留在 `docs/screenshots/full-acceptance-2026-09-19`；旧两轮证据、项目依赖和浏览器运行时也保留。上一轮被拒绝清理的空目录 `poker-session-qa-2d_m5hjd` 本轮未再次操作。

74的触控诊断事件：pointerdown命中cancel-board时scrollY=841，pointerup时scrollY=812，随后的click目标变为status-line。页面滚动发生在同一次触控按下/抬起之间；即时定位修复后，相同快速触控序列通过。末次F3失败文件为后续脚本动作基线时序问题，不能拿它当作此故障的原始完整日志。
