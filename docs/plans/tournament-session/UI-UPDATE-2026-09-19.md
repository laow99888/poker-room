# 三栏工作区与选牌反馈更新

## 交付范围

- 桌面宽度从 1100px 起恢复三栏：左侧建桌参数/本手参数/下一手设置/记录，中间庄位选择或牌桌及牌库，右侧操作台及推演结果。
- 窄屏保持单列：建桌时参数优先；开始后牌桌和选牌优先，设置移到后方。
- 金色外框标示当前操作模块，轮到英雄行动时推演区以青色边框强调；结算时操作台保持高亮。
- 首张底牌立即显示在牌桌和牌库预览区；公共牌未选满时也显示临时预览，取消后移除，不写入牌谱。
- 牌库显示已选数量、选中底色及按压反馈。选齐底牌后，已选两张牌保持清晰，其余牌禁用；进入公共牌录入后重新启用可选牌。
- 原位更新牌库按钮，保留键盘焦点；aria-pressed 表示选择状态，预览区通过 aria-live 宣告变化。
- 公共牌展示改为非交互元素，消除看起来可点击但没有操作的按钮。
- 熟人记牌仍默认关闭并收在更多设置。会话数据格式、计算算法及采样次数未调整。

## 验证结果

- `node --check frontend/app.js`：通过。
- `node --test tests/frontend/*.test.mjs`：54 项通过。
- `node tests/browser/workspace-ui.e2e.mjs`：11 类检查通过。桌面三栏检查 1100/1280/1440px；九人桌建桌及待选牌检查 320/375/390/768/1440px，未发现横向溢出或座位互相重叠；包含实际触控模拟与真实推演接口。
- `node tests/browser/session.e2e.mjs`：通过。覆盖四条街、未知余额、草稿恢复、保存失败回退、升盲、淘汰、多标签页冲突、网络/推演重试等。
- 已人工查看桌面参数、底牌选择、推演结果及 320px 九人桌截图。浏览器模拟不能替代 Android/iOS 真机验收。
- 本次未运行后端全量 pytest、扩展 acceptance.e2e.mjs 或性能压测；未修改后端。

证据目录：`docs/screenshots/workspace-ui-2026-09-19/`。`results.json` 为专项结果，`core/` 为连续记录回归证据。

## 本地预览

原有服务 `http://127.0.0.1:8765/app.html` 保留。刷新页面即可加载新布局，HTML 已更新静态资源版本参数。

## 资源收尾

- 本次隔离服务 PID 11320 已停止，8142 端口已释放。
- 本次浏览器 PID 26292、13684、1124 均已退出。
- 核对为空后已清理 `C:\Users\Administrator\AppData\Local\Temp\poker-session-qa-0g3ecyaz`。
- 截图和 JSON 结果为交付证据，保留在上述证据目录；本次没有失败记录。
