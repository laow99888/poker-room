# poker-tools 系统性审查报告

审查日期：2026-09-18。基线提交：`b78bb73e044f9c140ff7f249af12db2d8ab239b8`。

## 结论

**综合评分：56 / 100。定位：有一定工程基础的学习原型，尚不适合作为可信的实战决策或真实牌谱复盘工具。**

最重要的风险不是界面精细度，而是部分合法或无效输入会让计算无法结束、策略计算的数学语义存在错误、学习记录链路实际失败，以及牌局回放混入系统虚构的对手底牌。现有测试全部通过，不能据此推断完整用户流程正确。

建议先修复下列 P1 问题，再增加功能或开展公开多人部署。无需整体重写或迁移前端框架；保留 PokerKit、FastAPI、原生前端，修正领域边界和接口契约更划算。

本报告按用户要求审查完整快照，而非某个提交区间。README 是产品承诺参考；没有外部正式规格。按规范/工程质量和产品/算法语义两条线独立检查，最终按“编号、问题、发生场景、交付方案”汇总。未修改业务代码、配置或用户统计数据。

## 项目理解

核心路径为：前端保存配置、手牌与动作列表 → `/api/hand/view` 用 PokerKit 从头回放 → 返回当前行动者、可操作金额、公共牌和结算 → 轮到英雄时调用 `/api/hand/advice` → 完成后调用 `/api/stats/record`。

建议路径为：行为分类 → 位置范围图 → 对手画像收窄 → 蒙特卡洛胜平负 → 底池赔率/SPR 与启发式混合建议 → 牌面结构、范围优势 → 单挑 CFR 参考。ICM 独立挂在牌局视图上，并没有参与行动 EV 或建议优化。

存储边界：前端 localStorage 保存设置/代号；后端所有访问者共享 `backend/data/opponents.json`；牌局本身靠请求中的完整动作列表回放。`run.py` 绑定本地地址，但仓库另有公开域名的 Nginx 反代配置，因此“本地单人使用”和“公开多人服务”需要分别评估。

优点：

- PokerKit 承担下注顺序、合法动作、边池分配等复杂状态机；treys 承担牌型评估，避免全部自研。
- 后端按牌桌、范围、权益、画像、CFR、ICM 分文件，结构直观；无状态回放天然适合复现。
- Kuhn、AKQ 和 ICM 暴力枚举对照是有价值的算法测试，CI 同时包含 Python 和前端检查。
- CFR 限制范围和迭代量、提供降级结果，并明确声明翻牌/转牌是终值近似，方向合理。
- 原生前端和本地静态资源适合小型离线工具，当前规模没有迁移 React 或引入微服务的必要。

## 评分依据

分数为基于本次证据的工程判断，不是代码覆盖率或行业认证。

| 维度 | 得分 | 依据 |
|---|---:|---|
| 架构与领域边界 | 15 / 20 | 模块边界初步清晰，复用成熟牌桌引擎；未知牌、真实回放、模拟结算边界混淆 |
| 算法与业务正确性 | 10 / 25 | 有锚点测试，但 CFR 聚合、筹码口径、多家权益和行为分类存在实质错误 |
| 完整用户流程 | 11 / 20 | 录入、撤销、建议、统计功能齐全；异步覆盖、错误删历史、状态重置和记录链路失效 |
| 稳定性与安全 | 7 / 15 | 有基础校验和原子替换；无界循环、500 边界、HTML 注入、公开部署隔离不足 |
| 测试与质量门禁 | 10 / 15 | 78 项 Python 和 6 项前端测试通过；关键端到端流程与组合边界缺失 |
| 可维护性与交付 | 3 / 5 | 零构建易运行；依赖未固定，运行数据进入 Git，存在重复模型和文档偏差 |
| **总分** | **56 / 100** | **先修正确性和数据完整性，再扩展产品能力** |

## 问题清单

P1：高优先级，影响核心结果、数据完整性或服务可用性。P2：应计划修复，影响特定场景、鲁棒性或可维护性。带“条件”的问题依赖部署方式，不代表已验证线上暴露。

| 编号 | 问题 | 发生场景 | 交付方案 |
|---|---|---|---|
| 01 / P1 | 范围抽样可以永久循环，且多人结果依赖输入顺序 | 英雄 AsAh、公共牌 AdAcKd、对手 AA 无解时死循环；另一个合法场景仅交换对手顺序，胜率从约50%变约75%，枚举应为66.67% | 统一牌编码，预过滤已知死牌，校验联合范围可行性；使用有上限且分布正确的联合抽样，无法采样返回明确4xx；验收超时、枚举对账与对手排列不变性 |
| 02 / P1 | CFR 把桶内平均胜负当成必胜/必败 | 河牌一个桶混有强弱牌，平均符号为 -0.2，即 40% 权益，终值却按必败处理 | 河牌统一转成权益 `(v+1)/2` 后计算期望收益；覆盖混合桶、小范围精确解与默认 8 桶对照 |
| 03 / P1 | CFR 混淆剩余筹码与累计投入 | 底池 20BB、剩余筹码 10BB、无需跟注，合法下注被删除，只剩 check；面对已全下对手也可能直接不支持 | 单独表示历史底池、当前街投入、双方剩余筹码及合法动作；从 PokerKit 合法动作构树，所有街共享一致金额口径 |
| 04 / P1 | 学习记录完整链路失败 | 完成一手后 POST record 返回 500；前端先置 recorded=true 且不处理 HTTP 失败；部分重开路径不复位 | API 传 `player_intel` 的结构化结果；成功后确认记录；每手独立 ID 和状态；补“结束→记录→重开→再次记录→失败重试”接口/前端集成测试 |
| 05 / P1 | 虚构底牌阻止真实公共牌录入，并参与结算 | 未知对手被确定性分配 Kh 等牌，真实翻牌含 Kh 会被拒绝；摊牌展示/输赢以虚构牌决定 | 区分真实复盘与模拟模式。未知牌保持 unknown；真实结算需要实际摊牌或标记未确定；模拟模式明确牌的来源，公共牌不可被隐式占位牌锁死 |
| 06 / P1 | 多人平分与边池的权益/EV 错误 | 三家共用皇家同花顺按 50% 权益处理，实际为 1/3；短码以无法赢取的深筹码边池计算跟注赔率 | 返回每次试验的实际分池份额，按英雄有资格赢取的各底池分别计算 EV；覆盖三家平分、多层边池和不等额全下 |
| 07 / P1 | 翻后动作污染翻前分类 | BB 翻前过牌、翻牌下注，被标为翻前 rfi；跟注者翻后下注可变成 three_bet | 行为事件明确 street，只用翻前事件生成 VPIP/PFR/3bet 与翻前范围；另建翻后条件信息 |
| 08 / P1 | 范围“按强度收窄”的排序反转 | `_chen_score` 给 22 评分 14、99 评分 7、TT 评分 6，小对子可能优先于更强对子保留 | 修复点数映射，明确评分公式或采用经验证的起手牌排序；加入对子单调性及收窄前后权益测试 |
| 09 / P1 | 旧建议响应覆盖新牌局 | 建议尚未返回时重开、换位置或继续操作，旧响应直接写入当前 state.advice | 请求绑定 hand_id、revision 与输入快照，取消过期请求；写回时校验版本；测试逆序响应、撤销、重开 |
| 10 / P1 | 请求失败会删除合法历史 | view 连续返回 JSON 503，前端每次 pop 一步，三个合法动作可被全部删除 | 区分已确认日志与待提交动作；仅验证失败时撤销对应待提交动作；服务/网络失败保留历史并允许重试 |
| 11 / P1（条件） | 公开部署时统计读取/重置无身份和用户隔离 | 若使用仓库 Nginx 配置公开访问，任何能访问接口的人可读取全局统计或调用 reset | 明确本地/服务两种部署模式；公开服务加入身份、授权、用户数据隔离、计算配额和限流；验收未授权请求拒绝且用户间隔离 |
| 12 / P2 | ICM 用下注后的剩余筹码直接计算 | 等起始筹码刚贴盲注就出现不同奖金期望；任意玩家全下 stack=0 时整个面板返回错误 | 展示明确的手前或结算后快照；若要手中 ICM，按结果分支结算后加权，不能丢掉底池；处理淘汰及已锁定名次奖金 |
| 13 / P2 | 重置状态不完整，配置可能静默丢失 | 选公共牌时清底牌/换桌型，gridMode=board 而 view=null 导致 JS 异常；改盲注后 payouts 消失但输入框仍显示 | 统一初始化/重置函数，清理选牌、请求、记录标记；配置按字段更新并统一渲染；覆盖各种入口的状态迁移 |
| 14 / P2 | 用牌局内容哈希当作手牌身份 | 两手独立牌局只要 config/ops 相同，即使玩家或英雄不同，也被当重复；800条后裁剪又可让旧请求重记 | 以独立 hand_id 保证幂等，内容哈希用于校验而非身份；同手重试一次、不同手相同内容两次，持久化唯一约束 |
| 15 / P2 | 对手名称未经转义进入 HTML | 名字含双引号可截断 input value 属性，标签/事件属性原样进入生成的 HTML | 文本用 textContent、输入框用 value，必要模板进行上下文正确的转义；覆盖引号/尖括号。未验证跨用户攻击路径 |
| 16 / P2 | API 输入结构过宽，错误逃逸成 500 | hand_over 后再发牌、raise.to=[]、无加注权时 raise、combos=[[]] 都返回 500 | 使用带判别字段的操作模型、固定两张牌元组、有限数值校验和领域异常；预期输入错误返回统一 4xx，保留服务端诊断日志 |
| 17 / P2 | 范围优势计算允许两家持有同一张牌 | hero=[AsAd]、villain=[AsAd] 仍返回 50%；普通重叠范围会纳入不可能的组合对 | 对公共牌与双方底牌做统一冲突过滤，按合法组合对加权；没有合法对时返回不可计算；复用同一个范围快照到权益/RA/NA/CFR |
| 18 / P2 | 固定 seed 无法保证跨进程复现 | MC 将 set 转 list 后抽公共牌，Python hash seed 不同导致同参数结果变化 | 抽样前按固定牌序排列，使用局部 RNG，记录算法版本与 seed；子进程用不同 PYTHONHASHSEED 对账 |
| 19 / P2 | 第一层验证脚本失败仍可退出 0 | `_verify_layer1.py` 只打印 fails，没有非零退出，与 README“失败即非零退出”相矛盾 | 失败时 sys.exit(1)，加入受控失败的门禁测试；将关键锚点纳入 CI，避免只依赖人工读日志 |

## 关键证据与定位

以下为本轮实际执行或源代码直接确认的证据。模拟统计使用内存替身，没有写入真实 `opponents.json`。

### 01 范围循环

`backend/equity.py:174` 的 `while True` 没有失败出口，牌冲突仅重抽；`backend/ranges.py:52` 生成组合时没有统一点数字符大小写。独立进程调用 `simulate(['As','Ah'], [{'type':'range','text':'AA'}], ['Ad','Ac','Kd'], 1000, 42)` 超时；受控超时终止该复现进程。不能只增加重试次数：多家范围必须联合处理，否则后续范围无牌时仍会卡死或引入抽样偏差。

顺序偏差也已独立复现：英雄 KcKh，公共牌 5h7s9cJd3c；V1={AcAd,QcQd}，V2={AcKd,2h2d}。三个合法联合组合等权，英雄赢其中两个，精确胜率 66.67%。各运行10000次、seed=42，V1→V2 实测胜率 **49.98%**，V2→V1 为 **74.67%**。当前算法固定先选到的组合、只重抽后一家，会改变合法联合分布；应该整组拒绝冲突或显式计算条件权重。

### 02、03 CFR

定位：`backend/cfr.py:64`、`:99`、`:338`、`:355`、`:427`、`:443`、`:599`；上层剩余筹码来源 `backend/decision.py:54`。

复现代码：

```python
from backend.cfr import solve_river
from backend.ranges import expand_range

board = ['2c', '3d', '7h', '9c', 'Td']
r = solve_river(board, [('Ks', 'Kh')],
                expand_range('AA') + expand_range('QQ')[:4],
                pot_bb=2, to_call_bb=1, stack_bb=1.5,
                buckets=1, iterations=1600)
print(r['hero_strength'], r['avg_strategy'][(0, 0, '')])
# [-0.2], {'fold': 0.99999921875, 'call': 7.8125e-07}
# 40% equity, call EV = 0.4 * 2 - 0.6 * 1 = +0.2BB

r = solve_river(board, [('As', 'Ah')], [('Ks', 'Kh')],
                pot_bb=20, stack_bb=10, iterations=10)
print(r['root_labels'])
# ['check']
```

一桶案例用于隔离终值错误；同一取符号代码也用于默认多桶中包含混合结果的桶，不是“选择一桶导致的普通近似误差”。现有锚点常让每类手牌独立成桶，难以暴露该问题。翻牌/转牌不含未来行动的近似是已声明的设计限制，本报告未把这点本身当 bug。

默认 **8桶** 也已复现：board=5h7s9cJd3c、hero=KcKd；villain 取 `expand_range('QQ') + expand_range('TT')[:4]` 的10个较弱组合，以及 `expand_range('AA,JJ,99,77')` 过滤公共牌冲突后的前14个较强组合。`pot_bb=1.6,to_call_bb=1,stack_bb=1,iterations=2000` 时真实权益10/24=41.67%，跟注所需38.46%，EV=+0.08333BB，但求解输出 **fold=96.7271%、call=3.2729%**。

### 04 学习链路

`backend/app.py:158` 把 `_classify_lines()` 的 `{seat: str}` 传给 `record_hand()`；`backend/opponents.py:77` 对值调用 `info.get()`，实际异常为 `AttributeError: 'str' object has no attribute 'get'`。

配置 6 人桌、100/200、无 ante，英雄 BTN/AsAd，UTG、HJ、CO、BTN、SB 依次 fold，牌局正常结束。POST `/api/stats/record` 返回 **500 Internal Server Error**，模拟 `_save` 未被调用。现有 `tests/test_opponents.py` 直接传正确的 `player_intel`，所以单元测试通过而 API 路径失败。

前端对应 `frontend/app.js:471` 先设置 recorded，未检查 HTTP 成功；`:561` 的 reopen 没有重置 recorded。

### 05 占位牌与结算

定位：`backend/table.py:108`、`:195`、`:223`、`:251`；`frontend/app.js:84`。

6 人桌英雄 BTN/AsAd，UTG 跟注、其余非 BB 玩家弃牌、BB 过牌，发翻牌前视图已有 **13 张 taken**，其中包含未知对手占位牌与烧牌。BB 占位牌含 Kh；输入 `[Kh, 合法牌, 合法牌]` 会报“牌 Kh 已不在牌堆里”。最终 revealed/payoffs 也使用 dealt 的占位牌，因此“筹码守恒”只能证明虚拟局结算一致，不能证明真实牌谱结果正确。

### 06 多家权益与边池

定位：`backend/equity.py:143`、`backend/decision.py:297`、`:474`。

皇家同花顺公共牌 AsKsQsJsTs，英雄 2c3d、对手 4c5d 与 6c7d，精确枚举返回 win=0、tie=100。上层 `win + tie/2` 得到 50%，真实主池份额为 33.333%。应分别保留“发生平局概率”和“分池权益”。

另一例：100/200 无 ante，UTG 10000 全下、HJ 10000 跟注，CO 1000 待行动，视图 pot=20300、to_call=1000，输出 required_eq=4.69%。其中大部分是 CO 无权赢取的边池。若后面 BTN/SB/BB 均弃，CO 跟注后可争夺的主池仅 3300，门槛为 30.30%。本轮用 20% 权益替身隔离公式，现实现报 EV=+3260，实际在该后续分支为 0.2*3300-1000=-340。

### 07、08 画像与范围

`backend/decision.py:151` 遍历所有 operation 而不区分街。实测 BB 翻前 check 后 intel 为 pending，翻牌下注后变成 `{line:'rfi', kind:'open'}`。

`backend/decision.py:241` 的数字牌使用倒序索引计算点数；实测 `_chen_score(('2c','2d'))=14`、99=7、TT=6。此评分进入 `:257` 的强度排序，影响“紧对手”的保留范围。

### 09、10、13、15 前端

使用 Node VM 加载原始 `frontend/logic.js` 与 `frontend/app.js`，用受控 DOM/fetch 替身检查事件与请求顺序：

- `app.js:470`、`:479`：view 返回后挂起 advice，重开再释放旧响应，当前 advice 被写入 `{"stale":true}`。
- `app.js:455`：已有 3 个合法动作，连续返回 JSON 503，最终 ops=0、requests=4。
- `app.js:532`、`:606` 与 `:105`：board 模式清空 hero/view 后渲染，报 `Cannot read properties of null (reading 'street')`。
- `app.js:624`：已有 payouts=[500,300,200]，改大盲后 payload.config.payouts=[]。
- `app.js:375`、`:650`：名字中的 `" autofocus onfocus=alert(1) x="` 原样进入 input HTML；验证到注入字符串进入 DOM 模板，未在真实浏览器执行脚本。

### 11 公开服务边界

`deploy/ace.iiiu.im.conf` 将全部路由代理至本地服务；`backend/app.py:164`、`:169`、`:175` 没有鉴权；`backend/opponents.py:15` 使用全局单一文件。未访问实际公网地址、未执行 reset，不宣称现有线上实例已遭到攻击。

### 12 ICM

`backend/app.py:111` 只传各座位当前剩余 stack，不包含底池。6 人各起始 10000，100/200 无 ante，冠军独得 600，刚建局时显示 UTG/HJ/CO/BTN 各 100.5、SB 99.5、BB 98.5；这不是手前 ICM，也不是考虑底池归属后的结果期望。任一全下玩家 stack=0 时返回 `icm.error`，因此需要先定义快照语义，而不是简单允许零筹码。

### 14、16、17、18、19 其他证据

- `backend/opponents.py:36`、`:58`：签名只有 config/ops。内存存储中第一手 Alice 写入成功，第二手相同动作但 Bob/另一英雄返回 recorded=false。加入英雄或名字仍不能区分内容完全相同的独立手牌，应引入 hand_id。
- `backend/app.py:80` 使用 `ops: List[dict]`；`backend/table.py:168`、`:171`、`:188` 和 `backend/equity.py:103` 的输入边界未归一成领域错误。四个非法输入通过 TestClient 均复现 500。
- `backend/textures.py:88` 只检查双方与公共牌冲突，不检查双方互相冲突。双方均 AsAd、公共牌 2c3d7h9cTd，`range_equity(..., 100, 7)` 实际返回 50.0；该组合对根本不可能存在。
- `backend/equity.py:186` 从 set 生成抽样顺序。AsAh 对 KK,QQ,AKs、翻前、1000 次、seed=42，独立进程 PYTHONHASHSEED=1 得 win=81.7/tie=0.2，=2 得 win=80.5/tie=0.6；差异不含耗时字段。
- `_verify_layer1.py:135` 的失败分支只有 print，无 `sys.exit(1)`。这是静态控制流确认；本轮未运行该完整脚本，也未改动它注入失败。

## 更值得优化的设计

### 1. 明确真实牌谱与模拟牌局的领域边界

把手牌表示为 known/unknown；把结算状态表示为 confirmed/unresolved/simulated。系统不能把为了驱动状态机临时生成的牌当成真实观察。真实牌谱允许任意与已知牌不冲突的公共牌，收益只有在信息充分时才确认为事实。该边界应先于继续做自动牌谱导入。

### 2. 建立一份不可变的分析输入

建议构造 AnalysisContext，包含手牌版本、当前街、已知死牌、座位当前投入/剩余筹码、合法动作、可赢取的底池、推断范围和推断来源。当前 `decision.py:440` 收窄范围后，`:491` 又从静态图重建 villain_merged，导致权益与 RA/NA/CFR 不一定使用同一对手模型。一次构造后共享，能减少结果相互矛盾。

输出同时区分 showdown equity、实现权益假设、chip EV、ICM 奖金期望、启发式频率和抽象 CFR 频率。NA 目前是“两对以上占比”的代理指标，不能直接解释为真实坚果占比；建议改成反映代理含义的名称。河牌范围封顶、分桶和受限下注树下，也应表述为“抽象单街模型参考”，而不是完整真实德州的精确均衡。

### 3. 前端用明确状态迁移与请求协调

继续使用原生 JS，提取小型 reducer/状态迁移模块、API 请求协调模块、渲染模块即可。让开始一手、改变设置、选择牌、提交动作、撤销、记录成功都有单一入口；取消分散的 state 字段清空逻辑。分析结果必须绑定请求提交时的 hand_id/revision。加载、失败、过期和不可计算需要显式状态。

### 4. 将学习数据变成独立持久化模型

运行数据不应作为受版本控制的 `backend/data/opponents.json` 与代码一起发布。优先考虑 SQLite 事务、hand_id 唯一约束、schema version、显式数据目录与备份导出。现有 threading.Lock 只保护本进程 record，reset 不走同一锁，固定 `.json.tmp` 在并发写/多 worker 下存在竞争；`_load` 解析失败静默回空数据也可能掩盖损坏。

画像名称应作为数据字段，不能直接占用 `_seen`、`_pool_pos` 等存储元数据键。统计应区分“手数”和“有机会做该动作的次数”；当前位置池以所有 hands 为分母，不等同于机会条件下的 RFI/3bet 率。稀疏样本建议回退人群先验并展示样本量，而非把 5 手当作稳定画像。

### 5. 用契约测试连接已有单元测试

新增测试优先级：完整 record API；两手连续学习；建议乱序响应；view 503 保留历史；合法未知公共牌；短码边池 EV；多人平分；混合 CFR 桶；全下与不同 SPR 的合法动作；范围冲突可终止；跨进程固定 seed。

CI 锁定依赖版本并保留升级验证流程。测试不要只检查频率和为 100、筹码守恒、对象字段存在；要检查输出的业务含义和独立数学基准。特别是实际适配层与通用算法之间的契约，不能用算法锚点测试代替。

## 建议交付顺序及验收

| 阶段 | 范围 | 可验证的完成条件 |
|---|---|---|
| A：恢复核心完整性 | 01、04、09、10、13、16；公开服务同时处理11 | 请求有界返回；两手都能入库且重试不重复；任何网络失败不丢历史；旧响应不能写入当前牌局；输入错误稳定4xx |
| B：校正领域与数学 | 02、03、05、06、07、08、12、17 | 已知真实牌谱可以完整录入；多家/边池/混合桶与独立参考对账；所有推荐动作合法；ICM快照含义明确 |
| C：建立长期质量 | 14、15、18、19及存储/依赖/CI优化 | 名称安全渲染；独立手牌身份稳定；跨进程可复现；失败门禁可靠；数据与代码发布隔离 |

每阶段均保留现有 78+6 测试回归，并只增加针对真实缺陷的契约/性质测试。界面换框架、大规模目录重组、用户系统和自动牌谱监控不应抢在这些修复之前。

## 本轮验证与边界

- `python -m pytest tests -q`：**78 passed，110.37秒，2条依赖弃用警告**。
- `node --test tests/frontend/logic.test.mjs`：**6 passed**。
- `node --check frontend/app.js`、`node --check frontend/logic.js`：通过。
- 额外验证：TestClient 的统计/非法输入/ICM 场景，纯函数 CFR/多家平局/边池/画像/范围复现，Node VM 异步与事件复现，受控子进程死循环与跨进程 seed 检查。
- 环境：Windows；Python 3.14.7；Node 24.19.0；FastAPI 0.141.1、uvicorn 0.52.4、PokerKit 0.7.5、treys 0.1.8、pytest 9.1.1、httpx 0.28.1、Starlette 1.6.0。
- 未运行三个完整 `_verify_layer*.py`、`_stress_api.py`、`_fuzz_advice.py`；未做真实浏览器视觉/响应式/读屏验证、长时间压测、Nginx 实机验证或公网安全扫描。没有宣称移动端布局已通过验收。
- 现有套件未执行覆盖率测量；不能给出覆盖率百分比。UI 响应式和 Nginx `$connection_upgrade` 宿主配置依赖属于待验证项，未列为已复现缺陷。

## 文件与资源收尾

仅新增本审查报告；未修改业务代码，未创建提交，未写入用户统计。所有常规测试进程已自然退出；超时复现过程中仅终止本任务记录的 PID **31300、28540、29724**，收尾检查已不存在。未启动开发服务器或 watcher，未占用或释放服务端口；未新建需清理的任务临时目录。依赖和既有测试缓存保留；复现结果与未验证边界保留于本报告。

---

## 修复复核追加记录（2026-09-18）

### 复核结论

复核提交：`1fe51ad`，包含 `0340281` 与 `1fe51ad` 两次修复；比较基线为收录原报告的 `c94f0e0`。本次开始时工作区干净，原报告内容保留为历史记录。

**原19项中，6项通过本轮针对性验收，另外13项尚未完整解决。** 其中有已修复原复现场景但遗漏关联入口的，也有修复无效和新增回归的。不能以“89项Python测试、10项前端测试全部通过”作为19项全部关闭的依据。

下方追加编号20至32分别关联这13项未关闭的问题，方便继续交付；不是说原19项全部修好之后又新出现13个无关问题。本次没有重新计算综合评分，前文56分是原版本的历史评估。

### 原19项验收状态

“通过”仅指原问题在本轮已核查场景下成立，不等于对应模块所有输入均无缺陷。

| 原编号 | 状态 | 本次核对结果 | 追加编号 |
|---|---|---|---|
| 01 | 部分修复 | 死牌冲突及时报错，联合抽样顺序偏差已修；合法小写范围仍被误判为冲突 | 20 |
| 02 | 通过 | 混合桶使用权益期望，一桶及默认8桶回归通过 | - |
| 03 | 部分修复 | 显式money解决重复扣筹码；仍推荐非法加注，不足额全下跟注仍按足额处理 | 21 |
| 04 | 部分修复 | record API结构错误已修；旧手记录响应仍可把新手标为已记录 | 22 |
| 05 | 部分修复 | 占位牌避开声明公共牌；未知烧牌仍禁选，虚构底牌仍参与结算且前端未标识 | 23 |
| 06 | 部分修复且新增回归 | 引擎能返回真实平分份额；赔率漏算英雄已有投入，建议/UI未使用新份额，边池未逐层计算 | 24 |
| 07 | 通过 | 公共牌之后的动作不再改变翻前情报分类 | - |
| 08 | 通过 | 数字牌映射修正，22/99/TT/AA评分为5/9/10/20，原排序反转消失 | - |
| 09 | 修复无效 | 快照返回string，比较函数却读对象属性，实际不同牌局仍比较为true；view也无版本保护 | 25 |
| 10 | 部分修复 | 503不再删历史；所有400仍触发连续pop，已有view时错误提示被覆盖 | 26 |
| 11 | 部分修复，部署条件 | 模拟远程无令牌reset返回403；统计读取/写入仍未按用户鉴权或隔离 | 27 |
| 12 | 通过 | 使用手前筹码快照，等筹码均分正确，全下不再使面板报错，UI已说明手前口径 | - |
| 13 | 部分修复 | 明确重开/换桌型及奖金保留已改善；两个清底牌入口仍遗漏统一重置 | 28 |
| 14 | 部分修复 | 带不同ID的相同牌局可分别记录；首手ID为空，去重历史裁剪后旧ID又可重记 | 29 |
| 15 | 部分修复 | 名字输入框已转义；建议面板直接插入名字，浏览器已确认测试事件执行 | 30 |
| 16 | 部分修复 | 原错误raise/结束后发牌/空combos等改为400；非有限奖金和非法names仍返回500 | 31 |
| 17 | 未修复 | textures.py未改，双方同时持有AsAd仍返回50%范围权益 | 32 |
| 18 | 通过 | 不同PYTHONHASHSEED下同参数输出的胜/平/负/权益一致 | - |
| 19 | 通过 | 失败分支已加SystemExit(1)；完整第一层脚本本轮16项校验通过 | - |

### 待交付问题

本节代码行号均对应复核提交 `1fe51ad`。

| 编号 | 问题 | 发生场景 | 交付方案 |
|---|---|---|---|
| 20 / P2，原01 | 合法小写范围仍被拒绝 | `/api/equity` 同样输入仅把KK改为kk，200变400“联合冲突过多” | 在范围解析边界统一点数大小写、花色与组合顺序；用大小写混合输入做API等价性测试 |
| 21 / P1，原03 | CFR仍生成非法加注并错误处理短码跟注 | 对手已全下，PokerKit禁止加注，CFR仍79.3%建议raise；只剩2BB时跟注终点却假定可补9BB | 按双方可匹配金额和引擎合法动作建树，处理未跟注退回；不支持的局面明确降级，不输出错误策略 |
| 22 / P1，原04 | 上一手记录响应污染下一手 | 第一手record未返回时点“再来一手”，旧200随后把新手recorded置true，下一手不再记录 | 每条记录绑定hand_id及提交快照，异步回调只更新对应手；补延迟响应跨两手的集成测试 |
| 23 / P1，原05 | 虚构信息仍混入真实录入和输赢展示 | 尚未观察到的烧牌9d被禁选；AA对未知对手最终显示亏损，实际由虚构9h3h决定 | taken仅包含已知观察牌；真实模式未知摊牌不结算，模拟模式必须明确标识结果来源且前端消费该状态 |
| 24 / P1，原06 | 新EV公式漏算已在池的己方筹码，且份额未贯穿建议/边池 | 小盲补盲门槛25%被算33.33%；三家分池建议/UI仍按50%；赢边池但输主池时EV方向错误 | 把跟注相对弃牌的增量EV作为明确口径，按每个底池的参与者分别分配收益，所有消费者使用正确权益字段 |
| 25 / P1，原09 | 快照校验类型错配，旧view/advice仍覆盖新状态 | 不同string快照比较true；重开后空手牌仍显示旧建议；请求中改位置可得到英雄与视图不一致 | 用实际hand_id+单调revision贯穿view/advice；设置改变作废旧请求并提交新快照；测试真实helper调用契约 |
| 26 / P1，原10 | 400仍会猜测性删除历史；错误提示不稳定 | 持续400仍删除三步合法历史；503虽保留动作，可见状态却回到“轮到UTG” | 已确认历史与待提交动作分离，仅回滚明确被拒绝的操作；统一错误优先渲染并提供同快照重试 |
| 27 / P1（公开部署条件），原11 | 只保护reset，未完成用户数据隔离 | 模拟远程客户端无令牌仍可200读取summary/profile；record仍为全局写入 | 明确公开与本地部署能力；公开模式对读取/记录/重置统一鉴权并按用户隔离，验证代理链的真实客户端配置 |
| 28 / P2，原13 | 清底牌仍保留公共牌模式及旧手状态 | 公共牌模式清底牌后再点2c，触发Cannot read properties of null (reading 'street') | 将hero-pick/hero-clear纳入统一迁移，保留另一底牌但清选牌/记录/旧请求状态并生成新手身份 |
| 29 / P2，原14 | 首手没有ID，长期幂等仍失效 | 新页面payload.hand_id=null；记录800个后续ID后，重放首个ID又成功计入 | 初始化即生成ID；ID持久化唯一约束，与统计窗口分开保存；首手、清牌重建、长期重试均验收 |
| 30 / P2，原15 | 建议面板遗漏名称转义 | 名称包含img/onerror，经renderAdvice插入后浏览器执行测试标记 | 所有名字输出使用textContent或正确转义；测试实际renderAdvice，不能仅测试escapeHtml工具函数 |
| 31 / P2，原16 | names和非有限奖金仍可触发500 | names={BB:123}或{BB:'_seen'}记录返回500；payouts=['Infinity',300]或['NaN',300]视图返回500 | names使用受约束字符串映射并隔离元数据命名空间；金额要求有限数值；统一预期输入错误为4xx |
| 32 / P2，原17 | 范围优势仍计算不可能的底牌组合对 | hero与villain都仅AsAd，返回50.0 | 过滤双方实体牌交集，零合法组合返回不可评估；与合法联合枚举对账 |

### 关键定位与复现证据

#### 20：小写范围

定位：`backend/ranges.py:38`，`backend/equity.py:193`。

输入英雄AsAh、公共牌2c3d7h9cTd、1000次、seed=42，对手范围KK正常返回100%胜；换成kk返回400。解析器仍保留小写点数，而抽样牌堆用大写。无界循环已修，但合法输入等价性没有修复。原顺序偏差案例本轮两个顺序胜率为65.84%与66.75%，已接近枚举值66.67%。

#### 21：CFR合法动作与不足额跟注

定位：`backend/cfr.py:439`、`:443`，`backend/decision.py:64`。

案例A：100/200、无ante，BTN起始20000，其余10000，英雄BTN/AsAd。UTG/HJ/CO弃牌，BTN跟注、SB弃牌、BB过牌；公共牌2c3d7h、9c、Td，前两街双方过牌，河牌BB全下9800。引擎返回to_call=9800、min_raise_to=None、max_raise_to=None。以英雄AsAd、对手KsKh单组合调用 `_cfr_street_block`，仍返回supported=true、raise79.3%“加注到100BB”。对手已全下，没有合法加注空间。

案例B：

```python
solve_river(
    ['2c', '3d', '7h', '9c', 'Td'], [('Ks', 'Kh')],
    expand_range('AA') + expand_range('QQ')[:4],
    money=(1, 10, 0, 2, 0), buckets=1, iterations=1600,
)
```

权益40%，英雄已投1BB且只剩2BB，实际跟注2BB后争夺6BB、对手退回7BB，门槛33.33%；输出却fold=0.99999921875，call终点c0=c1=10。源码注释虽声明“不足额全下跟注不在骨架内”，但接入层没有拒绝这种局面，因此不能作为已妥善降级处理。

#### 22、25：异步响应仍会跨手写入

记录定位：`frontend/app.js:477`、`:484`。Node VM执行实际JS：旧手发起延迟record，重置后新手recorded=false，旧200返回后变true。应将记录确认状态绑定旧手ID，而不是修改全局当前手标记。

建议定位：`frontend/logic.js:41`、`:50`，`frontend/app.js:493`、`:501`。`handKey()`返回string；`sameHand()`访问两边的opsLen/handKey属性，实际均为undefined，因此两个非空string都相等。新增单测手工构造对象，未经过真正的handKey调用，掩盖了契约错配。

真实浏览器复现：英雄UTG/AsAd已获得建议，再发起建议请求并延迟响应；点击“重开”后底牌全空、view=null、advice=false；释放旧响应后底牌和view仍空，但advice=true，页面重新显示旧手43%左右胜率与行动建议。JSON证据为 `stale-confirmed.json`，截图为 `stale-advice-after-reopen.png`。

view定位：`frontend/app.js:445`、`:474`、`:655`。Node VM中BTN的view请求未返回时将位置改为UTG，重置后的refresh因busy跳过，旧view照常写入。最终selectedHero=UTG、viewHero=BTN，总共仅一次view请求。只修建议比较函数仍不足。

#### 23：未知烧牌与模拟输赢

定位：`backend/table.py:251`、`:275`、`:281`，`frontend/app.js:84`、`:253`。

100/200无ante，全员10000，英雄BTN/AsAd，前三家fold、BTNcall、SBfold、BBcheck后，view.taken=['9d','Ad','As']。9d是系统烧掉的牌而非真实观察，前端据taken将其禁用。直接通过API声明9d翻牌实际可以成功，故这次定位为UI禁选，不声称同一输入一定返回后端400。

随后真实录入公共牌2c3d7h/9c/Td、全程过牌，结束返回simulated=true，payoffs={SB:-100,BB:300,BTN:-200}，revealed={BB:['9h','3h'],BTN:['As','Ad']}。前端没有消费simulated标记，仍把虚构对手底牌决定的结果作为“各家盈亏”展示。只增加后端标记没有完成用户可见语义修复。

#### 24：赔率新增回归及权益消费者遗漏

定位：`backend/decision.py:310`、`:427`、`:439`、`:502`、`:508`、`:510`，`frontend/app.js:370`。

普通补盲：6人100/200无ante，四家弃牌后英雄SB待补100，当前池300含英雄已投100。权益替身为30%时，实际输出required_eq=33.33、ev_call=-10；正确跟注后总池400，门槛25%、EV=0.3*400-100=+20。英雄已投入的100虽为沉没成本，弃牌时会放弃对它的争夺；计算“跟注相对弃牌”的收益时不能从可赢池中删除它。

此次修复还把 `tests/test_decision_api.py:112` 的原普通底池期望改成错误的新分母2825，并把随机场景的精确赔率断言弱化为0至100区间。需要恢复独立数学基准，避免用改错测试预期来掩盖回归。

三家皇家同花公共牌：引擎已返回equity=33.33，但 `_build_recommendation` 仍使用win+tie/2=50；前端真实浏览器渲染同样显示“你的权益50.0%”。数据字段已经新增，消费者尚未全部迁移。

边池案例：100/200无ante，stacks=[10000,10000,1000,10000,10000,10000]，英雄CO/KsKh。UTG全下1000，HJ跟注，CO加注到2000，其余弃牌，HJ跟注；2c3d7h及9c两街HJ/CO均过牌，Td河牌HJ下注1000。当前各家累计投入[100,200,1000,3000,2000,0]、pot=6300、to_call=1000。只将对手范围替换成UTG AsAh、HJ QsQh，实际调用模拟器得到总体权益0%、EV=-1000；实际主池3300输给UTG，但跟注后的4000边池必胜HJ，跟注相对弃牌增益为+3000。必须按每层底池的竞争者分别计算，修分母不能独自解决。

#### 26：错误处理仍会改变历史或隐去错误

定位：`frontend/app.js:459`、`:467`、`:470`、`:209`。

Node VM受控请求：3条现有合法历史，连续返回400，仍触发4次请求并把ops清到0。400也可能代表配置、英雄手牌、某个较早操作错误，不能推断最后一步就是错误来源。503现在保留历史，但showError之后renderAll/renderConsole会覆盖可见状态行，界面重新显示“轮到UTG”，没有明确保留错误和重试入口。

#### 27：公开部署边界

定位：`backend/app.py:156`、`:169`、`:174`、`:186`，`backend/opponents.py:15`。

使用TestClient模拟客户端203.0.113.10，未发送令牌，reset返回403且模拟reset函数未调用，说明该保护有所改善；summary与profile仍返回200，record没有身份归属，底层仍共享同一个JSON文件。

本轮未对公网地址或Nginx实机发请求。Uvicorn对转发头的处理受可信代理配置影响，不能简单断言“经Nginx所有请求必然被当作localhost”。需要以实际部署验证为准；本项未关闭的确定原因是读取/写入和用户数据隔离未完成。

#### 28：实际点击仍可卡住选牌流程

定位：`frontend/app.js:532`、`:550`、`:558`。

真实浏览器执行：选择AsAd，UTG/HJ/CO弃牌、BTN跟注、SB弃牌、BB过牌，进入发翻牌模式；点击As底牌槽清牌，再点击牌库2c。控制台报 `Cannot read properties of null (reading 'street')`，状态为heroCards=[null,'Ad']、view=null、gridMode='board'、boardPicks=['2c']。操作台提示选底牌，但点击仍走公共牌分支。

仅给updateDeckTip增加view守卫避免了提前报错；实际card点击分支仍访问null.street。这两个清底牌入口必须走统一状态迁移。截图：`board-clear-failure.png`。

#### 29：首手与去重生命周期

定位：`frontend/app.js:58`、`:413`、`:690`、`:722`，`backend/opponents.py:62`、`:67`。

真实浏览器新页面 `payload().hand_id === null`，启动时没有调用生成ID的逻辑。刷新页面直接开始的每一手都可能回退内容签名去重；手牌ID应在初次初始化就建立，而不是等用户先点重开。

内存存储复现：记录hand_id='first'，接着记录800个不同后续ID，再提交first，返回recorded=true。原因是_seen超过800后裁剪到500，ID去重窗口随统计缓存丢失。应独立保存幂等键并定义保留策略；不能宣称无期限幂等。

#### 30：遗漏的名称输出点

定位：`frontend/app.js:376`、`:669`。

名字输入框已转义，但建议面板仍使用 `<b>${o.name}</b>`。浏览器中给建议数据的name设置 `<img src="/audit-missing-image" onerror="window.auditInjected=1">` 后调用实际renderAdvice，观察到window.auditInjected=1。此为无外部通信的测试标记；没有读写凭据，也没有声称已验证跨用户攻击。`browser-evidence.json`中nameInjection字段记录了结果。

#### 31、32：输入边界与范围冲突

31定位：`backend/app.py:40`、`:79`、`:96`、`:162`，`backend/opponents.py:83`、`:102`。结束牌局记录时names={BB:123}或{BB:'_seen'}均返回500；view传payouts=['Infinity',300]或['NaN',300]同样返回500。测试用内存存储替身，不触碰真实统计。应在Pydantic/领域边界明确类型、有限数值及存储命名空间。

32定位：`backend/textures.py:77`。`range_equity([('As','Ad')], [('As','Ad')], ['2c','3d','7h','9c','Td'], 100, 7)`仍返回50.0。没有任何合法实体牌组合对，应返回不可计算。该文件在两次修复中没有变更。

### 追加验收建议

优先修21、24、25、22，分别对应错误策略/EV、过期状态写入和跨手漏记；随后处理23、26、28，保证真实录入不被虚构信息或恢复逻辑破坏。公开部署前同时完成27。其余输入、转义、身份及范围一致性一起补契约测试。

测试应调用真实使用路径：`sameHand(handKey(stateA), handKey(stateB))`；从app.js触发click/change及延迟fetch；从API完整记录两手；用独立底池枚举计算期望。不要只给新helper写与调用方无关的单测，也不要削弱原本有效的数学断言。

### 本轮执行结果与证据范围

- `python -m pytest tests -q`：**89 passed，140.79秒，2条依赖弃用警告**。
- `node --test tests/frontend/logic.test.mjs`：**10 passed**；两份JS语法检查通过。
- `python _verify_layer1.py`：**16项校验全部通过**，失败分支的非零退出通过源码确认。
- 真实浏览器：Puppeteer + Chrome 148，1440×1000桌面视口，真实前端/真实view API，统计持久化被内存替身禁写；建议请求使用真实响应结构的受控延迟，注入验证只设置浏览器测试标记。
- Node VM补充：view位置切换竞态、record跨手响应、400/503错误恢复、真实helper调用契约。
- 后端补充：纯函数、真实回放和TestClient核查上述数学/范围/输入场景；模拟远程身份仅发生在本机TestClient，没有公网访问。
- 截图已打开检查，确实包含目标页面与异常状态。两张PNG分别约855KB、781KB，未压缩。证据目录：`docs/screenshots/recheck-2026-09-18/`。
- 旧建议复现以 `stale-confirmed.json` 和同名场景PNG为准。早期浏览器脚本的模拟JSON漏了advice外层，导致写入undefined；保留的 `stale-evidence.json`、`stale-debug.json`、`stale-assignment-trace.json` 是诊断记录，不作为最终缺陷结论。`browser-evidence.json`的清牌、初始ID、注入及权益显示字段有效，旧建议字段以最终复现覆盖。
- 未做移动端/触屏完整回归、全量视觉验收、Nginx实机验证、长期压力测试；本轮未重复完整第三/第四层推演脚本。没有宣称所有点击组合都已覆盖。

### 本轮文件与进程收尾

业务代码、用户修复提交与统计数据未修改，未创建提交；仅追加本报告并保留截图/JSON诊断证据。测试服务使用独立端口8876，且启动时把统计读写替换为内存空数据与无操作保存。

测试服务PID **32400**已停止，**8876端口已释放**。浏览器PID **17896、31984、23844、32864、32668**均通过browser.close正常退出并确认不存在；测试和浏览器安装命令均已退出。未按进程名批量结束任何服务。

为完成真实浏览器验证补齐的Puppeteer Chrome运行时保留在用户缓存目录；没有清理项目依赖、浏览器运行时或既有缓存。证据目录因确认存在缺陷而保留，没有删除用户数据或未明确归属的路径。

## 第二轮修复复核追加记录（2026-09-18，4aca5c4）

### 当前结论

本轮比较 `1fe51ad...4aca5c4`，核对上一轮20–32的交付要求。开始时工作区干净。上文两个版本的结论、截图及评分均为历史记录，不代表当前提交仍存在同样的全部缺陷。

**20–32中，7项原复现场景通过，1项按明确保留窗口有条件通过，5项部分修复。** 本轮确认9项需继续交付的问题，续编为33–41，其中7项P1、2项P2。它们包含原问题的剩余分支和本次引入的数据问题，不能简单相加为“41个同时未解决的问题”。

最重要的剩余风险是：CFR金额仍可能不可执行；短码赔率及多底池建议仍与正确EV冲突；旧异步响应仍能写回；用户学习数据未接入实际建议；公开部署默认全清与旧数据迁移重置存在风险。**不建议据测试全绿宣布本轮全部修复完成。** 本轮不重算综合评分。

### 20–32验收状态

“通过”指本轮核查的原触发场景通过，并非整个模块无缺陷。

| 编号 | 当前状态 | 验收结果与边界 |
|---|---|---|
| 20 | 通过 | kk与KK解析/API等价，合法小写范围不再被误判为冲突 |
| 21 | 部分修复 | 对已全下对手不再推荐raise；剩2BB的不足额跟注正确；累计投入映射成本街加注额仍错误，见33 |
| 22 | 部分修复 | 旧record成功不再把新手标为已记录；旧失败响应仍污染新手错误状态，见40 |
| 23 | 原场景通过 | taken只包含已知英雄牌与公共牌，未知烧牌不再禁选；页面明确标识模拟盈亏。仍是占位牌模拟结算，没有实现真实未知摊牌结算，属于当前能力边界 |
| 24 | 部分修复 | SB补盲25%正确；三家权益份额被消费；旧边池案例EV已变+3000，但赔率与主建议仍有两类错误，见34、35 |
| 25 | 部分修复 | string快照契约和跨手handId守卫已补；等长动作分支、读取响应体期间重开仍可写回，见36 |
| 26 | 原场景通过 | 已确认2步遇400仍保留2步；503保留待确认操作且显示重试。公共牌入口未接unconfirmed仍属流程不一致，见下方优化说明 |
| 27 | 部分修复并有回归 | 具名档案已按用户ID分区，但advice缺身份；缺管理令牌时远程全清重新开放；匿名ID本身不是认证，见37、38 |
| 28 | 通过 | 清底牌统一重置并保留另一张牌；公共牌模式、候选牌和记录状态清空，下一次选牌不报错 |
| 29 | 有条件通过 | 首手及清牌后ID非空；幂等键独立保存。源码明确5000条上限、裁剪到4000，超出窗口可重记；只能承诺窗口内幂等，不能承诺永久幂等 |
| 30 | 通过 | 实际renderAdvice输出已转义名字，测试标签不再成为原始HTML |
| 31 | 通过 | 非字符串names、非有限payouts返回422；保留前缀名字安全降级，原500场景通过 |
| 32 | 通过 | 双方仅持相同AsAd的范围对返回None，不再给出虚构50% |

### 本轮待交付清单

本节行号均对应 `4aca5c4`。Spec轴核对原交付要求；Standards轴检查存储迁移、默认安全行为与验收隔离，未把一般代码风格建议计入缺陷数量。

| 编号 | 问题 | 发生场景 | 交付方案 |
|---|---|---|---|
| 33 / P1，关联21 | CFR将累计投入显示成本街加注额 | BTN起始1000，翻前已投200，河牌引擎最大加注到800；CFR仍推荐76.5%“加注到5.0BB”，即1000 | 明确累计投入与本街raise-to转换；减去前街投入/ante；所有展示金额按PokerKit合法上下限校验，不能只过滤动作名称 |
| 34 / P1，关联24 | 短码赔率重新计入英雄赢不到的底池 | UTG/HJ各投入10000，CO仅剩1000；20%权益下EV=-340，但required_eq=4.69%、建议call100%；可争池3300对应门槛30.30% | 赔率使用跟注后可争夺池；将分层池计算作为唯一依据，普通补盲与短码全下同时验收 |
| 35 / P1，关联24 | EV已按边池计算，主建议却仍用总体摊牌权益 | 主池输给UTG AA、边池必胜HJ QQ时，当前EV=+3000，主建议却fold85%/call15% | 跟注判断消费各池期望收益或等价加权权益；河牌无后续行动时对账建议方向与独立EV，不让总体0%覆盖必胜边池 |
| 36 / P1，关联25 | 版本校验仍遗漏等长分支和响应体等待窗口 | 撤销CO fold改CO call后旧建议覆盖新建议；响应头到达后重开，旧JSON正文仍写回空手牌 | handId之外增加单调revision，覆盖动作/撤销/配置变化；每次异步读取完成后、写入状态前再校验，view/advice/错误路径使用同一规则 |
| 37 / P1，关联27 | 记录和建议使用不同用户身份 | stats带X-Player-Id，advice仅带Content-Type；后端建议回退local，用户能看见自己的样本却不能用于建议 | 统一需要画像的请求身份；验收同一用户record→summary→advice闭环，以及另一用户不读取该具名档案 |
| 38 / P1，公开部署条件，关联27 | 未配置管理令牌时远程全局清空重新开放 | 未设置POKER_ADMIN_TOKEN，模拟远程客户端POST reset得到200/reset=all；仓库公开反代部署没有强制配置该变量 | 显式定义部署模式并采用保守默认；普通用户只清本人，管理员全清需鉴权；补缺失/错误令牌、普通用户、管理员及代理部署验收 |
| 39 / P1，新增迁移问题 | 旧档案残留导致清空后复活 | 旧顶层alice迁入users.local后，reset_user删除local；下一次读取又从顶层alice恢复档案 | 迁移完成后删除已消费旧键并标识schema；迁移必须幂等；补旧数据迁移→记录→重置→重新加载回归 |
| 40 / P2，关联22 | 旧记录失败仍写入下一手全局错误 | 第一手record待返回时重开，旧503或网络异常给新手设置“本手可能未计入统计” | 成功、HTTP失败和网络失败都绑定原handId；失败/重试保留原payload，不把旧手记录状态写入当前手操作台 |
| 41 / P2，新增验收问题 | 新增API测试未隔离真实统计，另有恒真断言 | test_invalid_names_and_payouts_are_4xx中的有效record可保存真实_DATA；“is None or True”永远通过 | 使用全测试共享的临时存储fixture；执行前后校验真实统计不变；删除恒真断言，断言重置前后共享池实际快照 |

### 关键证据与定位

#### 33：累计投入与本街金额

定位：`backend/cfr.py:456`、`backend/decision.py:99`。配置100/200、无ante，BTN起始1000，其余10000，英雄BTN AsAd；UTG/HJ/CO弃牌、BTN跟注、SB弃牌、BB过牌。公共牌2c3d7h、9c，前两街双方过牌；Td河牌BB下注200。对手固定KsKh，实际调用 `_cfr_street_block`：引擎max_raise_to=800（4BB），CFR标签为“加注到5.0BB”，概率76.5%。骨架的raise_to是全手累计投入，直接作为本街目标金额输出不成立。

原不足额跟注案例 `money=(1,10,0,2,0)` 当前call=0.99999921875，且根只有fold/call；这一修复已认可，33不是重复报告旧的足额跟注问题。

#### 34、35：正确EV未贯穿建议

定位：`backend/decision.py:531`、`:535`、`:574`。短码案例UTG全下10000、HJ跟注10000、CO英雄1000待行动，盲注100/200、无ante。用20%权益替身检查独立金额公式：当前EV=-340、required_eq=4.69%、call100%。英雄跟注后的可争池为3300；1000/3300=30.30%，不可争的深层边池不能压低该门槛。此案例用固定权益隔离金额逻辑，不声称20%来自具体范围模拟。

分层案例沿用上一轮24：stacks=[10000,10000,1000,10000,10000,10000]，heroCO KsKh；UTG全下1000、HJ跟注、CO加注2000、其余弃牌、HJ跟注；2c3d7h及9c两街HJ/CO过牌，Td河牌HJ下注1000。仅替换范围为UTG AsAh、HJ QsQh，其余使用真实模拟。当前边池4000权益100%、EV=+3000已正确，但主建议仍fold85%、call15%，理由仍是权益低于所需。修复时不能再把EV公式和建议判定作为两套互不对账的规则。

#### 36：两条独立异步复现

定位：`frontend/logic.js:49`、`frontend/app.js:480`、`:481`、`:525`、`:532`、`:534`。

Node VM执行实际app.js事件及受控fetch：CO fold后等待建议，撤销并改成CO call；两次handKey均为 `AsKs|BTN|1`，新建议先返回、旧建议后返回，最终ops是call，advice却来自old-fold。只比较动作数量无法区分分支。

第二条是最后一次await的位置：view在读取JSON前检查handId；advice也在读取JSON前检查handId和handKey。延迟JSON正文期间切换位置或重开，读取结束后无二次检查，旧结果照常赋值。

真实Chrome已确认advice路径：先获取真实API响应作为数据，受控延迟其json()，点击实际“重开”按钮后 `cards=[null,null],view=null,advice=null`；释放旧正文后cards/view仍为空，但advice带回旧标记，页面重新出现44.3%胜率与跟注建议。此证据使用当前提交的实际页面和渲染器；延迟为测试控制，不宣称测得生产网络的发生频率。view路径由实际JS的Node VM另行确认，会把UTG的新状态写成旧BTN视图并清掉busy。

#### 37、38：身份链路与部署默认值

定位：`frontend/app.js:460`、`:526`、`:527`，`backend/app.py:161`、`:170`、`:203`。真实浏览器网络记录显示summary带匿名用户ID，而hand/advice的X-Player-Id为null；代码使缺失ID回退local，因此自己的具名画像不会进入该建议请求。

独立TestClient模拟远程203.0.113.10，环境中POKER_ADMIN_TOKEN为空，将reset_all替换为Mock：响应 `200 {"reset":"all"}`，Mock确实被调用。没有操作真实统计，也没有向公网发送重置请求。此项是缺配置时的确定回归；是否影响现有线上实例取决于其环境配置。设置令牌后匿名ID仍只是命名空间，不等于完成原27要求的公开服务认证授权。

#### 39：迁移后清空复活

定位：`backend/opponents.py:44`、`:49`、`:216`。迁移把顶层legacy档案写入users.local.named，却没有移除顶层旧键。用实际record_hand/reset_user路径配合内存JSON读写，正常计数5→6→7成立；reset_user后users为空，随后get_stats('alice')又返回hands=7。

本项只报告清空后复活，**不报告正常新增记录发生计数回退**：迁移中的浅引用会使正常记录同时更新旧、新两处，独立核查已排除该误判。

#### 40、41：失败回调与测试隔离

40定位：`frontend/app.js:513`、`:514`、`:521`。旧record成功已有handId检查，但HTTP失败与catch仍直接showError。实际JS复现重开后旧503到达，空底牌的新手error变为旧手的记录失败文本；下次renderAll会进入错误面板。

41定位：`tests/test_decision_api.py:375`、`:388`，`tests/test_opponents.py:156`。有效record调用未使用该文件的临时存储fixture；把_load替换为空存储、_save替换为Mock运行真实测试，确认产生一次保存，写入五个非英雄位置计数。当前工作区预存同签名，因此本轮全量测试后git状态仍干净；这不能证明在新数据目录或其他用户数据上执行测试安全。`assert opponents.get_pool("BTN") is None or True`恒真，不能验收共享池保持不变。

### 设计与验收优化

- 将可争夺底池、份额、跟注EV和所需权益建立在同一份分层计算结果上；动作建议消费它，不重复推导分母。CFR累计投入和UI本街金额需要显式转换边界。
- 前端请求统一携带身份与版本；状态变更统一递增revision，写回前检查最后一个await后的版本。不要继续为各个回调单独补条件。
- 公共牌点击仍直接push到ops（`frontend/app.js:566`），绕过pushOp的unconfirmed设置；400后会保留待撤销的board。当前已有错误提示和手动撤销，因此未把它另计为原连续删历史缺陷，但建议统一提交入口并增加公共牌失败恢复测试。
- 三家平分现在显示33.3%正确，但 `frontend/app.js:383` 仍标“含平局半分”；多人平分应写实际分池份额或删除括注。作为低风险文案优化，不占本轮缺陷编号。
- 29的5000/4000窗口是明确实现限制。若业务需要永久去重，应使用独立持久化唯一键；当前验收不把窗口内测试扩展成无限期保证。

### 本轮验证与收尾

- `python -m pytest tests -q`：**101 passed，220.89秒，2条依赖弃用警告**。
- `node --test tests/frontend/logic.test.mjs`：**11 passed**；app.js与logic.js语法检查通过。
- `python -X utf8 _verify_layer1.py`：**16项全部通过**。最初普通命令已完成16项，但在打印勾号时因Windows GBK输出编码异常退出1；改为显式UTF-8后退出0，不将该环境输出问题计为业务缺陷。
- 算法：真实PokerKit回放、固定权益替身和固定范围模拟分别核查金额、边池与CFR；RA冲突及短码call独立复验。
- 前端：Chrome真实页面核对首手ID、学习请求头、清底牌后再选牌、重开后的旧JSON正文写回；Node VM执行实际JS补充等长分支、view正文竞态、record失败、400/503及转义场景。未宣称完成所有点击组合、移动端响应式、触屏、全量视觉或生产网络验收。
- 存储/API：迁移和重置用内存/Mock隔离；没有实际调用公网重置。没有重跑完整第三/第四层脚本、长时间压力测试或Nginx实机部署。
- 本轮交付仅追加本报告；业务实现、既有截图、统计数据均未修改，未创建提交。测试前后真实统计文件没有git差异。未创建新的临时目录或截图文件；上述失败/异常的复现步骤与输出保留在本报告中。
- 本轮临时服务PID31364已停止，8876端口已释放；浏览器PID29016、28184均经browser.close退出并确认不存在。全部测试命令已结束，没有长期进程待处理；未按进程名批量结束服务。没有需要清理的本轮临时目录，既有证据和浏览器运行时保留。

## 第三轮修复复核追加记录（2026-09-18，0fca69b）

### 当前结论

本轮比较 `4aca5c4...0fca69b`，包含33–41修复、异步行为测试、全清单审计脚本和手机端适配。以第二轮复核记录为验收要求，分别核对功能实现和测试/数据约定。工作开始时已有未跟踪文件 `_stress_check.py`，未修改或执行它。

**33–41中，7项原复现场景通过，2项仍部分完成（36、41）。** 原CFR金额、短码赔率、边池EV与主建议矛盾均已修复；本轮没有确认这三项的新业务回归。剩余及新增问题续编42–45，共4项P2：1项前端配置竞态，3项验收工具/测试问题。历史编号不是当前未解决问题总数。

106项Python测试、15项前端测试均通过；这支持修复有效，但不能替代以下独立场景。尤其 `_verify_all.py` 的“全部PASS”目前不能作为41项全部关闭的依据。本轮不重算综合评分。

### 33–41逐项核对

| 编号 | 状态 | 当前证据与边界 |
|---|---|---|
| 33 | 原场景通过 | 正确设置BTN起始1000，真实引擎加注范围400–800；CFR输出全下76.5%、跟注1BB23.5%，不再出现非法5BB。现有新增测试配置有误，见44 |
| 34 | 通过 | 固定20%权益的短码案例：可争池3300、门槛30.30%、EV=-340，弃牌85%/跟注15% |
| 35 | 原场景通过 | 实际模拟UTG AsAh、HJ QsQh：总体权益0%，4000边池权益100%，EV=+3000；主建议变为全下40BB100%，不再主要弃牌。这里只验原“正EV却主要弃牌”问题，不宣称该全下频率是严格均衡解 |
| 36 | 部分修复 | 等长动作分支、view/advice正文等待期重开已受revision保护；奖金/筹码/名字编辑仍未全部接入版本纪律，见42 |
| 37 | 通过 | advice已携带statsHeaders；真实浏览器请求确认X-Player-Id非空，后端按该ID读取画像。此为匿名命名空间，不代表公开身份认证已完成 |
| 38 | 原场景通过 | 默认reset只清用户；只有已配置且匹配的管理令牌才能全清。不再把未配置令牌等同允许全清 |
| 39 | 通过 | 旧键迁入users并移除，reset_user保存后再读取不再复活。纯读取迁移仍只在内存发生，下一次写入才持久化；不将此重复迁移列成已修复缺陷的复发 |
| 40 | 原场景通过 | 独立补测recorded=false并确认真实record请求1次，延迟503跨手返回后error仍为空；成功/失败回调均有handId守卫。仓库40-C测试未真正触发请求，见44 |
| 41 | 部分修复 | 全局autouse临时统计fixture已加入，原直接写真实_DATA路径受隔离；共享池保留断言仍无法检出全清错误，另有环境变量残留，见44、45 |

### 本轮待交付清单

行号对应 `0fca69b`。42属于功能要求的剩余分支；43–45属于可执行验收与测试隔离问题。

| 编号 | 问题 | 发生场景 | 交付方案 |
|---|---|---|---|
| 42 / P2，关联36 | 配置编辑未推进revision，旧视图仍覆盖新设置 | view正文等待期间奖金600改1000，refresh因busy退出；最终配置1000、ICM仍600，只有1次view请求 | 所有影响payload的编辑统一作废旧版本，并保证新快照最终提交；或明确锁定不可修改的牌局设置。补奖金、座位筹码和对手名字的真实事件测试，不能只加revision后让busy永久保留 |
| 43 / P2，新增审计工具问题 | 全清单审计脚本失败仍退出0，且部分PASS仅代表字符串存在 | 实际只有36条check，34/35/36/37/40没有独立结果；给结果收尾逻辑注入失败项后仍退出0 | 执行实际对应验收或明确标注静态检查/复用证据；覆盖表完整列出41项状态；任一失败非零退出，并测试失败门禁 |
| 44 / P2，关联41 | 回归用例没有触发声称验证的缺陷 | 共享池样本不足，两次查询均None；40-C起始recorded=true导致没有record请求；CFR短码放错座位且跳过全下标签 | 为每个用例先断言前置条件及请求数；共享池使用真实快照或足够样本；recorded=false后延迟响应；BTN短码放索引5并断言max=800及全下分支 |
| 45 / P2，新增测试隔离问题 | reset测试残留管理令牌，污染后续用例环境 | 原环境未设置POKER_ADMIN_TOKEN，执行reset测试后仍保留tok-1；后续测试已不处于默认无令牌环境 | 用monkeypatch管理setenv/delenv，或finally同时恢复“原值存在”和“原值不存在”两种状态；测试前后环境一致并在不同顺序下验收 |

### 复现证据

#### 42：奖金配置与ICM视图不一致

定位：`frontend/app.js:467`、`:655`、`:661`、`:679`。实际页面先获得奖金600的真实view响应，再受控延迟下一次view的json()；通过实际cfg-payouts change事件改成1000，释放旧正文后得到：

```json
{"config":[1000],"viewTotal":600,"viewRequests":1,"busy":false,"revision":0}
```

Node VM执行实际refreshCore/refresh及配置事件也独立得到相同矛盾。change没有推进revision，refresh因busy直接返回，旧视图被视为有效。筹码input和名字input同样修改请求依赖而不推进版本，也没有安排刷新，需一起纳入统一流程。修复必须同时解决版本失效、busy释放和新请求调度，不能只补一个自增语句。

原36中动作/撤销分支现在确实推进revision，view与advice在读取JSON后再次检查；原40的跨手record失败也已独立通过，不重复报告这些已修触发。

#### 43：审计结果不是可执行门禁

定位：`_verify_all.py:1`、`:106`、`:209`、`:275`。AST统计得到36个check调用，缺少独立编号34、35、36、37、40。部分覆盖被备注为其他条目复用，但例如“36-A在测试文件中”仅是字符串检查，不等于运行前端行为测试；边池测试名称存在也不等于其通过。

本轮运行脚本实际输出“共36项：全部PASS”。另外只提取其原始收尾代码，在独立子进程设置 `results=[('99','injected failure',False)]`，输出“1项FAIL”但进程退出码仍是0。该验证没有修改脚本或业务代码。应保留有用的检查，同时准确区分执行成功、静态检查和未执行。

#### 44：三处前置条件不足

1. `tests/test_opponents.py:151`–`:157` 在记录前取得pool_before，随后只记录1手；get_pool默认至少10手，两次均返回None。内存变异验证把reset_user替换成reset_all，真实测试仍通过，说明共享池全清会漏检。应在记录完成后捕获实际非空池快照，再执行重置并对账。
2. `tests/frontend/async-guard.test.mjs:44`、`:91` 的40-C继承recorded=true，refreshCore不发record，因此延迟失败跨手测试没有真正覆盖回调。独立改用recorded=false并断言recordRequests=1，业务当前守卫通过；缺陷属于测试假覆盖。
3. `tests/test_decision_api.py:450` 将1000筹码放在CO索引4，而英雄是BTN索引5；`:472`只校验带“加注到”的标签，跳过“全下”。`_verify_all.py`同类案例沿用该座位配置。独立以正确BTN短码复验业务已经通过，但仓库测试应锁住原故障场景以免回归。

#### 45：测试进程环境未恢复

定位：`tests/test_decision_api.py:415`–`:443`。old_token缺失时为None，测试随后设置tok-1；finally仅在old_token非None时恢复。同进程运行真实测试函数后观察到token_after=tok-1。该值是测试常量，不是实际凭据；风险是用例相互影响和默认部署分支被漏测。

### 验证范围与资源记录

- `python -m pytest tests -q`：**106 passed，188.82秒，2条依赖弃用警告**。
- `node --test tests/frontend/logic.test.mjs tests/frontend/async-guard.test.mjs`：**15 passed**；两份业务JS语法检查通过。上述假覆盖仍单独披露。
- `python -X utf8 _verify_all.py`：**实际36条全部PASS**；其中调用的layer1脚本退出0。该结果受43描述的覆盖与退出码限制，不等于41项全部动态通过。
- 算法专项28项测试通过，并独立使用原真实牌局/固定权益或固定范围复核33–35；没有用错误的BTN短码测试配置代替原场景。
- Chrome检查实际页面的奖金竞态、advice身份头；320/375/390/768/1440宽度的初始页面未发现文档横向溢出。这里只检查初始布局，不宣称全部牌局状态、触屏操作、座位重叠和无障碍均通过。
- 未执行已有未跟踪的 `_stress_check.py`，未做公网重置、Nginx实机部署或完整长期压力测试。统计复现用临时数据或内存替身；浏览器服务的统计保存替换为空操作。
- 临时服务PID **23744**已停止，端口 **8876**已释放；浏览器PID **16716**通过browser.close退出。中断恢复后再次确认这些PID不存在、端口无监听。所有本轮测试命令均已结束。
- 仅编辑本报告，未创建提交。检查期间 `backend/data/opponents.json` 出现新增 `schema: 2` 的差异，来源未确定；保留该差异，不声称真实统计文件前后完全不变，也未擅自恢复。既有 `_stress_check.py` 保留未动。
- 迁移复现目录 `C:\Users\Administrator\AppData\Local\Temp\tmphw1to17b`（仅o.json）删除被自动审批策略拒绝，已保留，未尝试绕过。`_verify_all.py`自身通过mkdtemp创建的目录未提供路径且没有自动清理；不按临时目录时间范围猜测删除。没有批量清理用户目录、缓存或浏览器运行时。缺陷证据和验证输出已记录在本节。

## 锦标赛连续牌桌交付复核（2026-09-19，46fd318）

### 结论与范围

**不通过交付验收，不能认定P0–P5全部完成。** 已有连续会话、固定座位、手数、结算草稿与部分请求守卫；真实浏览器的六人弃牌获胜流程能够结算9900/10100/10000×4并进入第2手，英雄位置从CO变HJ。但核心锦标赛流程在短桌、发公共牌、淘汰、升盲和异常保存上仍有阻断。

本轮固定比较 `0fca69b...46fd318`，工作开始时Git干净。规格为 `docs/plans/tournament-session/README.md`、`contracts.md`、`acceptance.md`，对照交付声明 `RESULTS.md`。并行分别审查工程正确性和需求落实，主审补真实浏览器/API验证。以下编号沿用历史清单，**新增46–65共20项：11项P1、9项P2**；历史43–45仍有未完成分支，归入65说明，不额外重复计数。

P1表示阻断核心流程、金额错误或丢数据；P2表示功能缺失、错误展示、异常输入或验收失真。本轮不据测试通过率给整体重新打分，也不把模块已存在等同功能已交付。行号均对应46fd318业务代码。

### Standards：状态与数据完整性

| 编号 | 问题 | 发生场景 | 交付方案 |
|---|---|---|---|
| 46 / P1 | 网络失败后请求无法结束，动作可能永久禁用 | fetch拒绝/JSON读取失败时，request-coordinator.js:85、:124引用未定义currentSource，抛ReferenceError，错误回调0次、pending持续true | 异常分支使用统一实时来源守卫，仅清理本请求；finally也遵守请求归属。实际测试fetch reject、JSON异常、旧请求迟到，断网后有可用重试入口 |
| 47 / P1 | 保存失败仍正式进入下一手 | app.js:696先替换rt.session，:702才persist；模拟配额异常后phase=ready、handNumber=2、saveState=error，刷新却恢复旧手 | 候选状态完整保存成功后再替换正式内存状态。save返回结果必须被提交者处理；失败保留结算页、旧手号及全部输入。用实际commit入口故障注入验收S-06/L-04 |
| 48 / P1 | 旧结算请求能覆盖已经取消/重录的本手 | app.js:694等待prepare期间本手被修订，session.js:746仅核handId；独立调用真实replayHand后旧响应仍生成第2手 | 提交绑定sessionId、sourceHandId、sourceRevision、会话草稿版本及transactionId；每个await后复核，取消/重录使事务失效。提交中禁止产生另一笔交易，不能只disabled一个可能被重绘的按钮 |
| 49 / P1 | 多标签冲突提示没有真正阻止写入 | app.js:1281设置rt.conflict并声称阻止写入，但:367 persist仍调用存储；冲突为true时实测写入1次 | 在统一保存和换手入口强制检查冲突并禁止写入。处理不同会话覆盖、删除键及同时提交；真实双tab验证旧页拒绝加载最新后仍不能覆盖新余额 |
| 50 / P1 | 连续换座可以静默删除玩家和筹码 | session.js:572只看原始席位；5人6座时先后把P3、P4移到空6座均成功。:805覆盖目标后，P3仍active却无座位，下一手总额50000变40000 | 每次编辑基于已经应用前面所有编辑的候选名单；提交再次校验每个active玩家恰好一个座位、目标不可覆盖、总额符合转入转出。用这组独立金额回归，不能通过重算总额掩盖丢人 |
| 51 / P2 | 差额说明、成员调整和位置校准刷新会丢 | app.js:571的reason/accept、:620/:632/:643成员操作、:668校准只改内存，未调用保存 | 所有草稿修改通过统一版本化保存入口，提交前flush。保存失败明确提示，重绘不能丢未提交输入；逐项修改后立即刷新验证L-02，不能只测试余额输入 |

证据：[frontend-probe.mjs](../screenshots/glm-review-2026-09-19/frontend-probe.mjs)、[frontend-probe.json](../screenshots/glm-review-2026-09-19/frontend-probe.json)。46使用真实协调器加拒绝fetch；47–49用Node VM执行实际控制器函数，替换存储、网络与渲染；50直接调用真实领域模块。**VM验证不是浏览器端到端验收。** 51为已定位的事件链路缺失，需由新增刷新测试补齐。

### Spec：接口与引擎适配

| 编号 | 问题 | 发生场景 | 交付方案 |
|---|---|---|---|
| 52 / P1 | 2/3/4/5/7人只能建局，不能正常行动/推演 | table.py:165和decision.py:496仍调用旧positions_for。逐人数初始view均200，但首个合法fold和英雄advice均400：“桌型仅支持 [6, 8, 9] 人桌” | 将物理座位到引擎的plan贯穿动作、建议、统计入口，不能只在建局支持2–9。每个人数从真实HTTP入口至少打到翻后；单挑完整校验先后手及换手。对应P-03/P-04/M-03 |
| 53 / P1 | 空SB局建议使用错误位置和BB，金额出现None | decision.py:496仍按标准索引配范围，:338/:586仍读盲注向量[1]。capacity9、seat2..7、BTN7、无SB、BB2、hero3时，view给seat2=BB/seat3=UTG，advice却按SB/BB理解，并输出“加注到 None BB” | 统一使用plan中的名义BB、位置和seat_id，覆盖推荐/CFR/边池/M值全部金额路径；opponents、side_pots也映射物理seat_id。独立验证无SB的建议金额，不以view正确替代advice验收 |
| 54 / P2 | v2没有实际计算/显示决赛桌ICM | app.py:264生成view后直接返回；合法final_table+[500,300,200]请求200但无icm；app.js也无有效ICM渲染 | 用本手冻结起始筹码和奖金计算并返回带seat_id的ICM，接入界面。补开启/关闭、淘汰后奖金确认及转桌暂停，不能仅校验字段存在。对应A-11…A-13 |
| 55 / P2 | 开启熟人功能后，具名画像没有接入建议 | app.py:273调用advice_for时names固定None。传profile_names={p2:named-probe}且learning=true，实测get_stats=0、get_pool=5 | 按occupant到引擎位置映射传递名字，开启时恢复具名分析；关闭仍保持named/pool零调用。补位置轮转、换人和重新启用，不能只证明record落库 |
| 56 / P2 | v2缺少严格请求校验，非法输入500、金额被截断 | table_context.py:128、app.py:259/:272：icm=7、ops=[null]、iterations="bad"均500；合法加注轮次to=400.7返回200并下注400 | 为v2使用明确请求模型与严格整数金额，校验数组元素、类型、范围、总额及角色字段冲突；预期非法输入统一4xx且不可静默截断。对应A03/C01 |

证据：[backend-probe.py](../screenshots/glm-review-2026-09-19/backend-probe.py)、[backend-probe.json](../screenshots/glm-review-2026-09-19/backend-probe.json)。脚本逐项保留请求、状态码、响应及学习调用计数；统计读取用mock，真实_load/_save调用均0。

### Spec：真实页面与连续操作

| 编号 | 问题 | 发生场景 | 交付方案 |
|---|---|---|---|
| 57 / P1 | BB输入选项没有换算，筹码可能少两个数量级 | app.js:200直接取occ.chips；:1274切单位只重绘，chipsFromBB仅导入未调用。浏览器选BB，BB200输入50，建桌保存每人50筹码，期望10000 | 输入字符串按当前单位经统一转换器处理；非整数筹码先提示取整确认。单位切换保持精确筹码，显示单位选择实际影响展示。用真实表单验证C-01/C-02/C-03，不只测纯函数 |
| 58 / P1 | 普通六人桌无法通过界面发翻牌，河牌入口也错误 | app.js:28/:996把street当上一街；翻前行动结束后后端street=flop、actor=null，UI提示“null号座”、按钮“发翻牌（1张）”；选一张返回400“flop应一次发3张”。river在STREET_DEAL中缺失 | 后端输出真实待发张数/是否可发牌，前端依合法状态显示3/1/1张；actor=null时隐藏下注动作并进入发牌流程。完整浏览器走翻前→翻牌→转牌→河牌→结算，并保留400错误信息和可重试选牌 |
| 59 / P1 | 结算0筹码淘汰及结束本桌被prepare阻断 | app.js:694无条件prepare，:719明确保留0余额参与者；真实浏览器填P1=0、P2=20000、其余10000并确认，返回400“起始筹码须为正整数：0”。英雄淘汰或不足2人也先走不该调用的下一手prepare | 共用领域候选迁移，先排除淘汰者再准备下一手；英雄离开/淘汰及人数<2直接保存最终结算并ended，跳过prepare。金额0自动触发名单变化与位置预览，不能依赖额外手工leave规避 |
| 60 / P1 | 升盲及下一手奖金设置没有UI入口 | app.html:112的nextround-body始终为空；app.js只导入setDraftBlinds，未使用。浏览器展开下一手设置只有标题，不能在同桌100/200升为200/400 | 实现下一手盲注、每人前注、ICM草稿表单及明确生效边界，保持当前手冻结。真实录入升盲并换手验证余额不变、BB折算变化；不得把“领域函数可调用”视作可用功能 |
| 61 / P2 | 建桌预览后修改筹码，确认仍保存旧预览 | app.js:264优先用缓存rt.setupPreview，相关输入未使其失效。预览P1=10000后表单改20000，确认建桌实际仍为10000 | 任何影响建桌的输入使预览失效并禁用确认，或确认时重新采集且要求确认新预览；prepare期间锁定/版本化输入。实测金额、座位、盲注、学习开关都应遵守同一机制 |
| 62 / P2 | 默认关闭熟人功能仍占据建桌和成员布局 | app.js:135无条件生成代号框，:607成员入座同样显示代号；熟人开关还直接放建桌主表单。浏览器learning=false时6个代号框可见 | 默认首帧整块隐藏代号标题/输入及其布局空间，仅高级设置保留开关；开启后再显示，关闭移出键盘焦点序列。按U-01/U-02检查真实computed布局，不只检查旧learn-panel已删除 |
| 63 / P2 | 手间名单编辑没有完整可用的候选视图 | app.js:595始终渲染正式s.seats；leave之后本座仍显示旧玩家，无法直接在此座输入替换玩家。:609隐藏英雄换座按钮。manual_close没有初始化nextHandDraft（:463），成员按钮直接把null传给applyRosterEdit | 渲染应用全部rosterEdits后的候选名单，与正式本手结算分开；支持同一结算离桌后入新玩家、英雄换座、撤销候选编辑。自然结束/手工结束共用草稿初始化并保存；用M-04/M-05/C-17真实点击验收 |
| 64 / P2 | 未知真实结算被标“已核对”，牌桌仍显示模拟余额 | app.js:508只识别source="unknown"，领域实际给pending，走“已核对”；:920直接显示模拟state.stacks。F3恢复后P2/P6 finalChips=null、confirmed=false，但UI来源均“已核对”，牌桌显示10300/9800 | 统一可信度枚举；未知显示待核对、未确认不能标已核对。牌桌终局优先显示人工实际余额，未知时显示待核对或明确标记的已知投入/余量，不能裸露模拟分池后的余额为事实。U-12需检查牌桌与表格两处 |

浏览器证据：[browser-probe.mjs](../screenshots/glm-review-2026-09-19/browser-probe.mjs)、[browser-evidence.json](../screenshots/glm-review-2026-09-19/browser-evidence.json)、[翻牌阻断截图](../screenshots/glm-review-2026-09-19/flop-blocked.png)、[淘汰阻断截图](../screenshots/glm-review-2026-09-19/elimination-blocked.png)。57/58/59/61/62及普通六人换手均由实际DOM点击/输入和真实接口完成，统计关闭。63同时有代码链路与浏览器manual_close后nextHandDraft=null证据，不声称所有子分支都点通。

64的证据来自 [restoration-probe.mjs](../screenshots/glm-review-2026-09-19/restoration-probe.mjs)、[restoration-evidence.json](../screenshots/glm-review-2026-09-19/restoration-evidence.json)、[未知结算截图](../screenshots/glm-review-2026-09-19/unknown-settlement.png)。由于58阻断正常发牌，测试通过领域模块构造F3记录、写入独立浏览器存储，再刷新走真实view和恢复UI；这是恢复场景验证，不是假称手工点击完整摊牌。当前未知余额仍为空，尚未直接把模拟输赢自动写入下一手，缺陷是错误可信度/模拟余额展示。

### Standards：验收证据与历史问题

| 编号 | 问题 | 发生场景 | 交付方案 |
|---|---|---|---|
| 65 / P2，关联43–45 | “全部PASS”超出测试实际覆盖，已有假覆盖和环境污染仍在 | test_session_context.py:151/:153新出现`or True`、`if False else True`恒真断言；F4/F5直接调用PokerKit动作绕过52的HTTP故障。RESULTS把B-01…B-04与U-07/U-08都标PASS，但截图/协调器单测不能证明真实页面流程。旧45仍残留tok-1 | 删除恒真断言，101个验收ID逐项标实际执行/失败/未执行及入口证据；补本表各故障回归，不能仅调整报告文字或测试预期。43仍需区分静态/动态覆盖；44旧共享池None与CFR座位错误继续修；45用monkeypatch恢复存在/不存在两种环境 |

65进一步证据：本轮用临时统计文件、独立Python进程执行真实 `test_stats_reset_default_is_self_only`，原环境无POKER_ADMIN_TOKEN，结束后输出 `admin_token_after_test=tok-1`。因此RESULTS声称“finally恢复已确认”并不成立，缺的是原值不存在的分支。`tests/test_opponents.py:151`仍在记录前获取不足样本的pool_before；`tests/test_decision_api.py:450`仍把英雄BTN短码写到CO索引4。`_verify_all.py`虽已加入非零退出，但编号22等检查仍以测试描述/源码字符串存在判PASS，未完成43完整要求。它们不是本轮134个测试失败，而是这些绿测试无法捕获相应缺陷。

### 修复顺序与再次验收

1. 先修52/53引擎适配与58发牌，使每个人数能从真实页面打完一手；同时修57金额输入，避免错误筹码污染后续验证。
2. 修46–50状态/事务完整性及59淘汰/结束流程，再补51草稿恢复。不得用隐藏按钮或删除错误分支“通过”验收。
3. 补60升盲、54 ICM、61–64表单/成员/可信度UI及55具名恢复；默认关闭仍保持全部统计读取/写入门控。
4. 修56边界校验和65证据质量；101个用例逐条核对。六人全弃和手工结束不能替代完整翻后/单挑/人员变化验收。

重新交付至少展示：6人完整到河牌、3人淘汰转2人且再换一手、英雄淘汰正常结束、下一手升盲、配额失败不换手、多tab旧页不能写、断网后恢复、50BB真实保存10000、默认熟人代号不占布局。测试/截图必须反映实现结果，未运行项明确标未运行。

### 本轮验证与资源记录

- `python -m pytest tests -q`：134 passed，161.50秒，2条依赖弃用警告。
- `node --test tests/frontend/*.test.mjs`：43 passed。7个协调器、25个session和11个旧logic测试；旧logic.js当前页面不加载，其通过不等于新app.js事件通过。
- 独立后端、领域/VM、真实浏览器复现见上述脚本与JSON。证据脚本退出0表示采集/确认复现成功，**不是产品验收PASS**。
- 浏览器采用独立Puppeteer会话和本地8141隔离服务；1440×1000桌面和375×812手机查看。截图已打开检查；手机牌桌公共牌占位与左下座位重叠、桌面结算栏文字过窄需在UI修复后专项复测，本轮不另计编号，也不声称B-04完整通过。
- 未跑长时间 `_stress_check.py`、全部101场景、物理手机/软键盘、生产部署或完整所有算法专项脚本；已发现核心阻断，本轮没有用这些未执行项目作通过依据。
- 真实 `backend/data/opponents.json` 前后SHA256一致：`9fc73f972ac7cfadd3aa8d60c7a398fb40d5ac183bc367acaf0d239ab859974e`。测试使用pytest临时fixture、mock或服务专用临时文件；没有恢复/覆盖用户统计。
- 隔离服务PID14340已停止、8141端口无监听；本任务浏览器PID33736、37696、31556、22288、18900均经browser.close关闭并确认不存在。原有8765服务未操作。服务用Ctrl+C结束，命令返回1属于主动停止，不是功能测试失败。
- 失败证据及复现脚本保留在 `docs/screenshots/glm-review-2026-09-19/`。其中probe-failure.png来自首版脚本错误等待第二手旧actor=3的超时，不计产品故障；修正为等待实际新actor后连续换手通过。浏览器入口脚本当前已完成无failure输出。
- 已核对并清理仅含测试opponents.json的临时目录 `C:\Users\Administrator\AppData\Local\Temp\poker-glm-review-4ashhbue`；删除后路径不存在。失败证据目录、项目依赖和浏览器运行时保留。
- 本轮只追加审查文档和独立复现证据，未修改业务实现、原交付报告、用户统计或创建Git提交。

## 2026-09-19 Codex修复结果（46–65，兼简化建桌）

用户授权直接修复，明确不使用子代理。本轮已修改46–65对应实现，详细逐编号结果与依据见 [修复交付](../plans/tournament-session/FIX-RESULTS-2026-09-19.md)，原始复核表保留便于追溯。

- 建桌收敛为6/8/9座，默认坐满、自己在底部、点选庄位自动推导盲位；支持BB输入，其他筹码预填为估算。默认隐藏熟人代号，高级设置保留功能。
- 修复2–9实际人数回放/建议/记录、空小盲名义BB、ICM与画像接入；严格拒绝畸形请求及小数加注静默截断。
- 修复3/1/1公共牌、未知结算来源、手间升盲/奖金、0余额淘汰和英雄结束、候选名单、草稿恢复及旧预览。
- 修复保存失败仍推进、过期事务、连续换座覆盖、网络异常锁和多标签页写冲突；已确认余额只在保存成功后成为下一手。
- 删除恒真断言，修正共享池快照与BTN短码、令牌环境隔离；`_verify_all.py`改为实际运行行为测试，不再以源码字符串命中宣称验收通过。

最终：158项Python测试、49项Node测试通过，聚合脚本退出0；真实浏览器主流程通过（6/9座、完整四街、手动余额、升盲、淘汰、英雄结束、保存失败回退、刷新恢复、多标签页、网络及建议失败重试、ICM）。1440桌面与390手机截图已打开检查，九座无座位框重叠。证据位于 [本轮截图与结果](../screenshots/session-fix-2026-09-19/results.json)。九人桌默认2万次模拟曾超过30秒，首轮采样调整为3000（>=7人）/5000（其他人数），界面显示实际次数，后续浏览器流程通过。

本结论不是“101场景全部PASS”：逐ID覆盖记录见 [验收矩阵](../plans/tournament-session/ACCEPTANCE-STATUS-2026-09-19.md)。未测真实手机软键盘、全部尺寸组合、长时间压力及生产部署；已保留最后一次超时截图/日志。真实opponents.json哈希仍为 `9fc73f972ac7cfadd3aa8d60c7a398fb40d5ac183bc367acaf0d239ab859974e`。

本次隔离服务PID1048停止、8142释放；浏览器均已关闭。任务空临时目录 `poker-session-qa-2d_m5hjd` 的删除被自动审批规则拒绝，已保留。未操作原有8765服务PID16608，没有创建Git提交。

## 2026-09-19 全面验收补充（66–74，已修复）

用户要求以手机为重点完成全面验收，本轮未使用子代理。补测新增以下9组问题，已直接修复并回归：

| 编号 | 问题 | 发生场景 | 交付方案 |
|---|---|---|---|
| 66 / P2 | 开启熟人功能后换手不自动发送记录 | 提交锁仍持有时调用同步，被直接跳过 | 释放锁后同步；真实浏览器验证原payload/UID及仅一次请求 |
| 67 / P1 | 嵌套损坏存档可能造成恢复白屏 | participants、结算行、历史或任务里有null等错误结构 | 保存/加载共同校验嵌套结构，保留原文并提供恢复入口 |
| 68 / P2 | 旧配置预填丢失容量和筹码精度 | 9座、10001筹码被预填成6座或按一位BB舍入 | 恢复容量，以整数筹码预填，熟人仍关闭 |
| 69 / P2 | 未知余额未填完时下一手仍可点击 | F3只填写一位未知玩家 | 剩余未知行时按钮disabled，填齐后启用 |
| 70 / P2 | 手机九座与公共牌布局碰撞、选牌热区小、恢复后牌库展开 | 320px九人桌及已有底牌的手机刷新 | 约束座位/桌心宽度，44px触控目标，16px输入，恢复时折叠牌库；45种组合验证 |
| 71 / P2 | 实际余额与牌桌显示不同步，结算还显示行动徽标 | 在结算表输入真实余额 | 局部刷新牌桌，输入不失焦；非playing隐藏行动徽标 |
| 72 / P1 | 结束牌谱取消结算后无法纠正动作 | 点击取消又被view自动送回结算 | 增加明确的结束牌谱纠错状态，可撤销或返回结算；固定身份/手数/正式余额 |
| 73 / P2 | v2混用旧index字段漏校验 | seat_id与index同发 | 统一4xx拒绝；真实HTTP回归 |
| 74 / P2 | 手机快速取消选牌漏响应 | 自动平滑滚动期间按下/抬起落点改变 | 改即时定位、触控操作策略与焦点恢复；真实触控完整四街通过 |

最终验证：176项pytest、54项Node通过；原浏览器回归和新增17组浏览器通过。101个验收ID中99项在注明层级通过，B-04/U-11的45种桌面模拟组合通过、真实手机键盘与系统行为待确认。完整报告、规格简化差异、截图、失败脚本时序说明见 [全面验收报告](../plans/tournament-session/FULL-ACCEPTANCE-2026-09-19.md) 与 [更新后的覆盖矩阵](../plans/tournament-session/ACCEPTANCE-STATUS-2026-09-19.md)。没有用模拟替代实机结论，也没有运行长时间生产压力或部署。

统计文件SHA256仍为 `9fc73f972ac7cfadd3aa8d60c7a398fb40d5ac183bc367acaf0d239ab859974e`。隔离服务PID37308已停止、8142释放，记录的浏览器进程全部退出，原8765/PID16608未操作。6个本轮测试临时目录的删除被自动审批规则拒绝（仅返回blocked by policy），目录保留，具体路径见全面验收报告；依赖与失败证据保留，未创建Git提交。

## 2026-09-19 庄位续手及布局回归（75–76，已修复）

用户提供位置校准报错截图并授权修复及系统测试。本轮确认是程序生成的候选不满足自己的校验，新增记录如下：

| 编号 | 问题 | 发生场景 | 交付方案 |
|---|---|---|---|
| 75 / P1 | 候选庄位与盲位顺序冲突，阻断下一手；错误草稿刷新仍存在 | 当前1–4号有人、BTN4、无SB、BB1，程序错误建议BTN3/SB1/BB2；SB离桌和新人插入也可触发 | 候选优先保留合法旧SB（可为空庄位），无旧SB参考旧BTN，冲突则从首个盲位逆向找前一入局者；旧草稿可“重新生成位置”，保留金额和身份，非法组合不能标为已核对 |
| 76 / P2 | 位置标签增加后部分尺寸牌桌重叠 | 320px六人桌座位碰公共牌，768px九人桌行动座位碰邻座 | 调整手机桌心宽度、牌间距与桌面桌高；新增含估算与行动徽标的30组布局检查，原45组也通过 |

最终：176项pytest、62项Node、16项算法基础检查通过；新位置专项10组、布局专项30组、原核心/扩展17组/三栏UI/加注专项通过。21,022组领域组合覆盖候选合法性及淘汰后真实换手事务。完整证据、恢复操作、验证边界与资源清理见 [位置修复与回归报告](../plans/tournament-session/POSITIONS-FIX-2026-09-19.md)。真机、生产压力未验收；真实统计哈希不变，8142已释放，原8765服务未动。一个成功测试浏览器临时目录删除被自动审批系统拒绝，已记录并保留；未使用子代理或创建提交。

## 2026-09-19 千手深度测试（77–79，已修复）

| 编号 | 问题 | 发生场景 | 交付方案 |
|---|---|---|---|
| 77 / P2 | 清空对手余额后恢复估算按钮缺失 | 快捷弃牌核对页原先全员已填；删除对手金额，无法直接沿用估算 | 估算入口随草稿动态显隐；自己的未知金额仍必须实际填写 |
| 78 / P2 | 修改淘汰结果使人员草稿冲突后，撤销入口消失 | 0余额腾出座位并加入新人，再把原玩家金额改正为非0 | 错误状态保留撤销人员变动按钮；只清人员与位置草稿，不丢已填余额 |
| 79 / P2 | 无响应请求使快捷弃牌/创建下一手/局面更新持续等待 | 网络请求挂起，既不成功也不立即失败 | 三类局面操作请求设15秒超时、释放锁并允许重试；超时不推进手号、不重复扣费、不追加未确认动作；采样设置不变 |

1000手真实浏览器连续记录通过：950手快捷弃牌、50手手动余额、40次单人校准、25次刷新、10次升盲；自己的余额与独立账本逐手一致。额外120手完整牌局回放通过。最终312项pytest、74项Node，新增6组边界及已有快捷12组、位置10组、扩展17组（含45种布局）及核心浏览器流程通过。

范围、修复前证据、最终JSON、资源清理和限制详见 [千手深度测试报告](../plans/tournament-session/DEEP-TEST-2026-09-19.md)。当前历史只保留100手摘要，账户/云端未实现；8765仍为旧后端，正式测试前须重启。真机/整天运行/100人并发未验收。本轮8142与所启动浏览器已关闭；两处测试临时目录删除被自动审批拒绝并保留，统计哈希不变，没有创建提交。

## 2026-09-19 手机空壳反馈（80，本地修复，线上待确认）

| 编号 | 问题 | 发生场景 | 交付方案 |
|---|---|---|---|
| 80 / P1 | 模块加载失败时静默显示空壳，开始按钮点了无反馈；模块依赖缺少同步版本号 | 注入缺少新版导出的旧session.js或阻断主脚本下载，真实页面座位0、牌库0、无可见报错，与用户截图一致 | 统一依赖资源版本、新增经典启动保护脚本、可见错误与重载、初始化前禁用建桌、旧内核/禁JS提示；8类启动场景、74项Node、12组快捷浏览器回归通过 |

实际访问用户域名时正常；随后用户提供`app.js?v=20260923qf`导入`session.js`缺少`calibrateStartingChips`的实际报错，已确认主脚本与依赖版本不匹配。按该准确函数缺失补测后通过。本轮未部署线上，也未在用户原Via设备确认恢复，完整同步文件及发布后核验要求见 [手机启动排查报告](../plans/tournament-session/MOBILE-STARTUP-2026-09-19.md)。隔离服务和浏览器已关闭，8142释放；空测试目录删除被自动审批拒绝并保留，证据及原有用户数据保留。
